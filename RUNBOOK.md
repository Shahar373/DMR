# RUNBOOK — סשן שטח: "האם התחנה מפענחת, ואם לא — למה"

מדריך הרצה מסודר ל-Pi 5 + RSP1B. הוא בנוי כ**חצייה בינארית**: כל שלב שולל
סיבה אחת, ומריצים את הבא רק אם הקודם ירוק. זה מכוון — התסמין "אין שיחות
בממשק" נגרם משלוש סיבות שנראות זהות מבחוץ (אין אות / האות לא DMR / השרשרת
שבורה), ובלי סדר מפרידים ביניהן בניחוש.

**רקע — מה כבר ידוע (נמדד בסימולציה, ר' CHANGELOG v0.17.0):**
- ‏IQ ב-8 ביט: ‎−26 dBFS פר-ערוץ = פענוח נקי, ‎−34 = שולי, **‎−38 = 10% שגיאות-סמל**.
  הדיווח מ-27.07 (‎−36..−39 dBFS) יושב **על הצוק**. שלב 1 נועד לאשר או לשלול את זה.
- ‏`gain` אינדקס 0–28 עולה עם הרווח (לא הפוך). שלב 2 מאמת את זה על החומרה שלך.
- ערוץ-שכן ב-12.5kHz שובר את multi עם ברירת-המחדל; ב-25kHz (הפריסה שלך) לא.

**כלי חדש שנועד בדיוק לזה:** `webtune/rf_probe.py` — מקליט IQ גולמי, מריץ
אותו דרך שרשרת ה-DSP **האמיתית**, ומודד **פקיחת-עין** (כמה רחוקים הסמלים
מארבע הסטיות התקניות של DMR). זה עונה על "יש אות תקין?" בלי לחכות
ש-DSD-FME יסכים לפענח, ובלי לדעת מה שודר.

---

## שלב 0 — הכנה (2 דקות)

```bash
cd ~/DMR && git fetch origin && git checkout claude/rf-decoding-stability-tests-igemcb && git pull
sudo ./install.sh                      # אידמפוטנטי; פורס גם את rf_probe.py החדש

# משחררים את ה-SDR + מטאטאים יתומים מריצות קודמות (⚠ קריטי — יתום מחזיק
# את ה-SDR והכל ימות ב-bring-up עם 0 אירועים)
sudo systemctl stop dmr-dsdfme
sudo pkill -f rsp_tcp; sudo pkill -f rsp_fm; sudo pkill -f dsd-fme; sleep 2
systemctl is-active sdrplay            # חייב active
```

---

## שלב 1 — האם יש בכלל אות, ומה איכותו? (10 דקות) ⬅ **הכי חשוב**

בחר ערוץ **בודד** מהסקר עם עוצמה "גבוה מאוד" — התחל ב-**164.3** (`vhf164_3`,
CC7). ⚠ אל תתחיל ב-multi: הוא מוסיף 3 משתנים (רוחב-פס, היסטים, N מפענחים)
לבעיה שעדיין לא בודדה.

```bash
sudo python3 /opt/dmr/webtune/rf_probe.py capture \
     --freq 164.3 --seconds 8 --out /tmp/iq_164_3.bin
python3 /opt/dmr/webtune/rf_probe.py analyse /tmp/iq_164_3.bin
```

הפלט נותן שורת-מדדים ואז **הכרעה + פעולה**. איך לקרוא אותה:

| ההכרעה | מה זה אומר | הצעד הבא |
|---|---|---|
| `אין אות בפס כלל — רעש בלבד` | שיא-הספקטרום ≈ החציון. אין שם כלום. | אנטנה/חיבור/תדר. **אל תחפש באג-תוכנה.** |
| `אות מתחת לרצפת-הכימות` | ‏<‎−34 dBFS. 8 ביט לא מספיקים. | אנטנה/מיקום/מגבר-קדם. אין פתרון בתוכנה. |
| `אין מודולציית DMR בתדר הזה` | יש נשא, אין סמלים. | ערוץ שקט — חזור בשעת תעבורה. |
| `אות מעוות — האנרגיה החזקה אינה בערוץ` | הפלט אומר **כמה kHz** מהערוץ יושב השיא. | הפרעת-שכן, או שהתדר האמיתי הוא מקום-השיא. |
| `אות DMR שולי` | צפויות שגיאות FEC. | שפר אנטנה/רווח, ואז שלב 3. |
| **`אות DMR תקין — העין פתוחה`** | ה-RF בסדר גמור. | ⇒ **שלב 3**. אם DSD-FME עדיין לא מפענח, הבעיה אינה RF. |

חזור על זה על 2–3 ערוצים (למשל `164.5375`, `164.725`) — אם כולם אותו סיפור,
זו בעיה מערכתית (אנטנה), ולא ערוץ ספציפי.

---

## שלב 2 — האם הרווח בכלל משפיע? (5 דקות)

הבעיה עם `/api/gain`: לפרוטוקול rtl_tcp **אין readback**, ולכן הממשק מציג
"מה פקדנו", לא מה קרה. זו המדידה היחידה שסוגרת את זה:

```bash
sudo python3 /opt/dmr/webtune/rf_probe.py gain-sweep --freq 164.3
```

מקבלים טבלה של עוצמה מול אינדקס 0/7/14/21/28. **מה לבדוק:**
- העוצמה **עולה** עם האינדקס ⇒ הכיוון תקין (כפי שנקבע ב-v0.16.2).
- אם היא נעצרת/רוויה לפני 28 ⇒ זה ה-gain האפקטיבי המקסימלי בפועל.
- אם `clip` מטפס מעל 0.05 ⇒ ירדת רחוק מדי, קח צעד אחורה.
- אם היא **לא זזה בכלל** ⇒ פקודות הרווח לא מגיעות — בדוק הרשאות
  ה-control-socket (הבאג של v0.16.1).

בחר את האינדקס הטוב ביותר (עוצמה מקסימלית **בלי** clip) והמשך איתו.

---

## שלב 3 — האם התחנה מפענחת שיחה אמיתית? (30–60 דקות, תלוי תעבורה)

זו השאלה שמעולם לא נענתה מקצה-לקצה. **הרץ רק אם שלב 1 חזר "תקין"/"שולי".**

```bash
sudo systemctl start dmr-web 2>/dev/null; sleep 2
curl -s -X POST localhost:8080/api/mode -H 'Content-Type: application/json' \
     -d '{"mode":"dmr","system":"vhf164_3"}' | head -c 400; echo

# לולאת מעקב — הרץ בטרמינל נפרד ותן לזה לרוץ בחלון תעבורה
watch -n 10 'curl -s localhost:8080/api/health | python3 -m json.tool | grep -A6 decode_state; \
             curl -s localhost:8080/api/rf | python3 -c "import json,sys; d=json.load(sys.stdin); \
             print(\"errors/min\", d[\"errors_per_min\"], \"| level\", d[\"level\"], \
             \"| parser_miss\", d[\"parser_miss\"])"'
```

ובמקביל, ה-log הגולמי — **זה מה שמפריד "סנכרון בלי FEC" מ"אין סנכרון"**:

```bash
sudo journalctl -u dmr-dsdfme -f | grep -Ei "sync|FEC|Color Code|Voice|Slot"
```

**נקודות-הכרעה:**

| מה רואים | פירוש | הצעד הבא |
|---|---|---|
| `decode_state: silent` + אפס שורות ב-journal | הרשת שקטה או אין נעילה | חכה לחלון תעבורה; ודא שוב שלב 1 |
| `Sync: +DMR` ואז `FEC ERR` בכל פריים | "יש אות, אין פענוח" | שלח לי את הפלט + הקלטת IQ מהשלב 1 |
| `parser_miss` עולה | הפרסר מפספס שורות אמיתיות | שלח לי את השורות מה-journal |
| שיחות מופיעות ב-`/api/dmr` | ✅ **התחנה מפענחת** | ⇒ שלב 4 |

```bash
curl -s localhost:8080/api/dmr | python3 -c "import json,sys; d=json.load(sys.stdin); \
  print(len(d.get('msgs',[])), 'כרטיסים'); [print(m) for m in d.get('msgs',[])[-5:]]"
```

---

## שלב 4 — multi (רק אחרי ששלב 3 ירוק)

```bash
curl -s -X POST localhost:8080/api/mode -H 'Content-Type: application/json' \
     -d '{"mode":"multi","system":"multi_164cluster"}' | head -c 300; echo
sleep 45
curl -s localhost:8080/api/rf | python3 -m json.tool | head -60
```

**מה לבדוק ב-`/api/rf`:**
- `level_by_channel` — כל 6 הערוצים מעל ‎−34 dBFS? ערוץ שיושב נמוך משמעותית
  מהשאר הוא ערוץ שלא יפוענח, ולא באג.
- `clip_frac` — מעל 0.25 ⇒ **הורד** רווח. ב-multi ה-ADC חותך את סכום כל
  הערוצים, והאינטרמודולציה פוגעת הרבה לפני שהיא פוגעת בערוץ בודד.
- `by_channel` — `status`/`restart_count` פר-ערוץ. `restarting` חוזר על אותו
  ערוץ = בעיה אמיתית באותו ערוץ.

ובמקביל, ניתוח offline של כל הערוצים מהקלטה **אחת** רחבת-פס:

```bash
sudo systemctl stop dmr-dsdfme
sudo python3 /opt/dmr/webtune/rf_probe.py capture --freq 164.415625 \
     --iq-rate 672000 --seconds 8 --out /tmp/iq_multi.bin
python3 /opt/dmr/webtune/rf_probe.py analyse /tmp/iq_multi.bin --iq-rate 672000 \
     --center 164.415625 --freqs 164.10625,164.3,164.325,164.5375,164.6375,164.725
```

זה נותן שורת-הכרעה **לכל ערוץ בנפרד** — כלומר תשובה ישירה לשאלה הפתוחה
מ-Phase 7 ("למה רק 2/6 ערוצים ייצרו אירועים"): אם 4 ערוצים חוזרים
"אין אות"/"מתחת לרצפה", זו תעבורה/קליטה ולא באג ב-multi.

---

## שלב 5 — A/B של `scaled_taps` (אופציונלי, נמוך בעדיפות)

הסימולציה כבר הכריעה: במרווח 25kHz (הפריסה שלך) **אין הבדל**; ההבדל
מכריע רק ב-12.5kHz. הרץ את זה רק אם שלב 4 הראה ערוצים מעוותים עם שיא-אנרגיה
של שכן קרוב:

```bash
sudo systemctl stop dmr-dsdfme
sudo bash scripts/spike-dmr-multi multi_164cluster 120          # 121 taps
sudo bash scripts/spike-dmr-multi multi_164cluster 120 scaled   # 339 taps
```

⚠ הדגל עובר כארגומנט-מיקום `scaled` — **לא** כמשתנה-סביבה (sudo מנקה אותם).
השוואה בעלת-ערך רק בחלון תעבורה עמוס.

---

## מה לשלוח לי בסוף

1. פלט מלא של `rf_probe analyse` מכל שלב שהרצת.
2. טבלת ה-`gain-sweep`.
3. ‏20–30 שורות מ-`journalctl -u dmr-dsdfme` סביב שיחה (אם הייתה).
4. ‏`curl -s localhost:8080/api/rf` ו-`/api/health` המלאים.
5. אם משהו נראה חריג — **קובץ ה-IQ עצמו** (`/tmp/iq_164_3.bin`, ~4MB/שנייה
   ב-240kHz). זה הנכס היקר ביותר: ממנו אפשר לשחזר את הבעיה offline, להוסיף
   אותה כפיקסצ'ר-רגרסיה, ולתקן בלי גישה לחומרה.

---

## תזכורות בטיחות-תפעול

- **SDR אחד בהחלפה.** לפני כל שימוש ב-`rf_probe`: `sudo systemctl stop dmr-dsdfme`.
- ריצה שנקטעה משאירה יתומים ⇒ הריצה הבאה מתה ב-bring-up (t≈9s, 0 אירועים).
  תמיד `pkill` לפני (שלב 0).
- לחזרה למצב עבודה: `curl -X POST localhost:8080/api/mode -d '{"mode":"off"}'`
  ואז המצב הרצוי — או פשוט דרך הממשק.
