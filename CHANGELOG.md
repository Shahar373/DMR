# Changelog

כל השינויים המהותיים לפרויקט מתועדים כאן. הפורמט מבוסס על
[Keep a Changelog](https://keepachangelog.com/he/1.0.0/), והגרסאות עוקבות אחר
[SemVer](https://semver.org/lang/he/).

## [Unreleased]

## [0.18.1] - נתפס בסשן השטח הראשון: rf_probe לא שרד הרצה שנייה

**נתפס תוך דקות מהשימוש הראשון בכלי (28.07):** ההקלטה הראשונה עבדה,
וכל ההרצות שאחריה נפלו ב-`Connection refused`. `gain-sweep` הצליח למדוד
אינדקס אחד בלבד ואז מת.

### Fixed
- **★ ‏`terminate()` חוזר מיד — אבל `rsp_tcp` עוד מחזיק את הפורט ואת
  ה-SDR.** ה-`rsp_tcp` הבא עלה, נפל בשקט על SDR תפוס, והלקוח קיבל
  `Connection refused` בלי שום רמז לסיבה. `RspTcpSession` (context
  manager חדש) **מחכה בפועל** לשחרור הפורט לפני שהוא חוזר, ומוסיף שהות
  לשחרור ה-USB. בנוסף הוא מזהה פורט-תפוס **לפני** ההרצה ומחזיר את
  הנחיית-הניקוי המדויקת, במקום stack trace.
- **‏`gain-sweep` מריץ עכשיו שרת אחד** ומחליף רווח בתוכו במקום להקים
  `rsp_tcp` לכל אינדקס. זה גם מה שהופך אותו למדידה בת-השוואה: אותה
  נעילה, אותו רגע-אוויר, רק הרווח משתנה.

### Changed
- **‏`gain-sweep` מודד גם את ה-AGC**, כשורה ראשונה באותה סריקה. זו השאלה
  שהמדידה הראשונה בשטח העלתה: מה ה-AGC בעצם עושה לעומת ידני — ואי-אפשר
  לענות עליה בלי לראות את שניהם באותה טבלה.

375 בדיקות (374→375).

## [0.18.0] - כלי-אבחון שטח: להפריד "אין אות" מ"אות מעוות" מ"שרשרת שבורה"

**נולד משאלה תפעולית:** "מה להריץ בסשן הקרוב". התשובה חייבת להיות רצף
שבו כל שלב **שולל סיבה**, ולזה היה חסר כלי: `/api/rf` נותן עוצמה
(`level`) ותדירות-שגיאות (`errors_per_min`), ושניהם לא מפרידים בין אין-אות,
אות-שאינו-DMR, הפרעת-שכן, ושרשרת-DSP שבורה. כולם נראים "אין שיחות בממשק".

### Added
- **★ `webtune/rf_probe.py` — כלי CLI לאבחון RF בשטח.** מקליט IQ גולמי
  מ-`rsp_tcp` (מפעיל אותו בעצמו), מריץ אותו דרך `rsp_fm.NfmDemodulator`
  **עצמו** — לא עותק — ומודד **פקיחת-עין**: מרחק-RMS של הסמלים מארבע
  סטיות ה-DMR התקניות (±1944/±648 Hz). זה מדד שלא דורש לדעת מה שודר,
  ולכן עובד על אוויר אמיתי ועונה "יש אות תקין?" בלי לחכות ש-DSD-FME
  יסכים לפענח. שלוש תת-פקודות: `capture`, `analyse`, ו-`gain-sweep`.
- **`band_spectrum()` — המפריד היחיד בין "אין אות" ל"אות מעוות".** בשני
  המצבים הדיסקרימינטור מרייל ומדד-העין נראה זהה. שיא-הספקטרום מעל
  החציון נמדד ב-~1 dB בפס ריק מול 24–70 dB בכל אות אמיתי (גם ‎−38 dBFS),
  ו-`peak_offset_hz` מצביע ישירות על מקור-האנרגיה — בהפרעת-שכן הוא יוצא
  בדיוק במרווח-הערוץ.
- **`gain-sweep`** — מודד עוצמה בפועל בכל אינדקס רווח. לפרוטוקול rtl_tcp
  אין readback (ולכן `/api/gain` מדווח `readback:false`), אז זו הדרך
  היחידה לענות "האם הרווח בכלל משפיע, ומה המקסימום האפקטיבי".
- **`RUNBOOK.md`** — סשן שטח מודרך כחצייה בינארית: הכנה → אות → רווח →
  פענוח חד-ערוצי → multi → A/B. כל שלב עם פקודות מדויקות, טבלת
  נקודות-הכרעה, ותנאי-מעבר לשלב הבא.
- **`tests/test_rf_probe.py` (14 בדיקות)** — כיול כל סף מול תרחיש-אוויר
  ידוע, כולל אימות ש-`EYE_GOOD_HZ` מסכים עם ה-SER שנמדד באותו תרחיש.

### Fixed
- **ערוץ ריק בתוך פס פעיל דיווח "תקנו אנטנה".** נתפס בבדיקת-שפיות של
  הפלט (לא בסקירת-קוד): בניתוח multi של 6 ערוצים רוב הערוצים שקטים רוב
  הזמן, וכולם קיבלו "מתחת לרצפת-הכימות ⇒ אנטנה/מיקום/מגבר" — הנחיה
  שמובילה בדיוק לפעולה הלא-נכונה. עכשיו כשיש בפס אות חזק שאינו בערוץ
  הנבדק, ההכרעה היא "הערוץ הזה ריק — הפס עצמו פעיל", עם המרחק בפועל
  לשיא. הטענה "אנטנה" נשמרת רק כשה**פס כולו** חלש.

374 בדיקות ירוקות (360→374).

## [0.17.0] - אפיון RF בסימולציה: איפה השרשרת נשברת, ומה זה אומר על הדיווח מהשטח

**מנה שנולדה משאלה, לא מדיווח:** "איך מגיעים ליציבות טובה יותר ב-RF
ובפענוח". התשובה שנבחרה — לא לנחש, אלא **למדוד**: `tests/dmr_signal.py`
(v0.16.0) כבר יודע לייצר אות DMR תקני ולהזין אותו לשרשרת ה-DSP האמיתית,
אז הורחב לכלי-מדידה ונבנתה סוויטה שממפה את **הגבולות** של השרשרת, לא רק
את התקינות שלה. שני ממצאים הפכו טענות קיימות בתיעוד.

### Added
- **★ `tests/test_rf_characterization.py` (15 בדיקות, ~6ש'):** עקומת
  SER↔SNR (כולל דרישת **מונוטוניות** — עקומה קופצנית מסגירה באג-מצב
  ב-DSP, לא "קליטה גרועה"), תקציב שגיאת-תדר (250/500/1000 Hz + בדיקת-
  תאום שמוודאת שהניסוי אינו חסין-מדי), סלקטיביות מול ערוץ-שכן
  (12.5/25/50 kHz × 0–30dB, 121 taps מול `scaled_taps`), רצפת-הכימות של
  IQ ב-8 ביט, וחיתוך — כולל ההבחנה בין ערוץ-בודד ל-multi.
- **שתי בדיקות קוראות את קבועי-הסף היישר מ-`index.html`** (`LEVEL_TARGET_LO`,
  `CLIP_BAD`) ומוודאות שהם עדיין מכוילים למדידה. עד היום הכיול הזה חי
  בהערה בקוד בלבד; עכשיו שינוי-סף בלי מדידה מפיל בדיקה.
- **כלים משותפים ב-`tests/dmr_signal.py`:** `mix()` (חיבור אותות עם
  שמירת יחס-עוצמות ונרמול הסכום — מבודד *סלקטיביות* מ*חיתוך*),
  `demodulate()`, `symbol_error_rate()`, `random_symbols()`. ה-helper
  הפרטי של `test_rsp_fm.py` עבר להשתמש בהם — מדידה אחת, לא שתיים
  שנסחפות.

### Fixed (תיעוד — הקוד לא שינה התנהגות)
- **★★ "‏`scaled_taps` נבדק ונמצא מיותר" (v0.16.0) היה נכון למרווח שנמדד
  בלבד.** המדידה ההיא בדקה **היסט 21 kHz** והסיקה ממנה על הכלל. במרווח
  **12.5 kHz** (המרווח התקני של DMR) המסקנה מתהפכת: ב-672kHz עם 121 taps
  שכן ב-12.5 kHz הורס את הפענוח כבר בעוצמה **שווה** — SER 23% — ו-62%
  כששכן חזק ב-10dB. עם `scaled_taps(672k)`=339 אותו מקרה הוא **0%**,
  זהה בדיוק לתוצאת המנוע החד-ערוצי (240kHz/121). הסיבה: רוחב-המעבר הוא
  ~3.3·fs/taps ⇒ 6.5kHz ב-240kHz מול 18kHz ב-672kHz.
  ⚠ **מה זה לא אומר:** זה אינו ההסבר לכשלי ה-FEC של v0.14.0 (שם הסיבה
  הייתה פאזת הדצימציה, ונשארת נכונה), ואינו נוגע לפריסות הפרוסות היום —
  המרווח המינימלי בסקר-השדה הוא 25 kHz, ושם 121 taps עומדים גם בשכן
  חזק ב-30dB (נמדד). **ברירת-המחדל לא שונתה** (הדגל נשאר opt-in) — זו
  החלטה שממתינה לאישור, לא שינוי שמוברח במנת-בדיקות.
- **★ רצפת-הכימות מסבירה את הדיווח מהשטח.** ה-IQ מ-`rsp_tcp` הוא 8 ביט,
  ולכן לאות חלש לא נשארת רזולוציה **גם בלי רעש כלל**: ‎−26 dBFS נקי
  לחלוטין, ‎−34 שולי (0.03% שגיאות-סמל), ‎−38 כבר 10%. הדיווח מ-27.07
  (‎−36..−39 dBFS פר-ערוץ ב-gain מקסימלי) יושב **בדיוק על הצוק** ⇒ זו
  בעיית-אות (אנטנה/מיקום/מגבר), ואין לה פתרון בתוכנה. סף האזהרה ‎−25
  שב-UI אומת כשמרני-נכון (בסף עצמו הפענוח עדיין 0% שגיאות).
- **★ סובלנות-החיתוך תלוית-מצב — הבחנה שלא הייתה מתועדת.** ערוץ בודד
  סופג `clip_frac` של 0.38 ב-0% שגיאות (FM הוא מעטפת-קבועה), אבל ב-multi
  ה-ADC חותך את **סכום** הערוצים ומייצר אינטרמודולציה שנופלת בתוך ערוצים
  אחרים: 6 ערוצים שווי-עוצמה כבר ב-`clip_frac`≈0.26 מייצרים ~2%
  שגיאות-סמל. הסף האדום 0.25 ב-UI נכון **ל-multi** — אסור להרפות אותו
  לפי מדידת ערוץ-בודד.

360 בדיקות ירוקות (345→360), זמן-ריצה כולל ~19ש'.

## [0.16.2] - תוקן בתיעוד: אינדקס-הרווח 0–28 אינו הפוך (היה חשד בשטח)

**נתפס בשטח:** משתמש עם gain=28 (מקסימום לפי ה-UI) ועדיין קליטה חלשה מאוד
פר-ערוץ (‎−36 עד ‎−39 dBFS). לפני שממשיכים לחפש בעיה בקוד, נבדק חשד: האם
"28" באמת מקסימום, או שקנה-המידה של SDRplay הפוך (כפי שהערה ישנה ב-§8
טענה) ו-28 הוא בעצם **מינימום**?

### Fixed (תיעוד בלבד — אין שינוי קוד)
- **ההערה הישנה הייתה מטעה.** נבדק מול המקור האמיתי של `SDRplay/
  RSPTCPServer` (`rsp_tcp.c`): "קנה-המידה הפוך" נכון **רק** לפרמטר הגולמי
  `gRdB` (gain *reduction* — ערך קטן=gain גדול, זו סמנטיקה תקנית של
  SDRplay API), אבל **לא** לאינדקס 0–28 שהקוד שלנו חושף. לפי טבלאות
  `rsp1b_vhf_gains_*` בפועל: אינדקס 0 → `LNAstate=9, gRdB=59` (gain
  הכי-נמוך), אינדקס 28 → `LNAstate=0, gRdB=20` (gain הכי-גבוה שהטבלה
  מאפשרת). **האינדקס שלנו עולה עם ה-gain בפועל — לא הפוך.**
- המסקנה בשטח: gain=28 הוא באמת מקסימום, ה"חשד" נפסל, וקליטה חלשה
  במקסימום-gain מצביעה על אנטנה/מיקום — לא על באג-כיוונון בקוד.

## [0.16.1] - נתפס בשטח: בקרת AGC/רווח נכשלה תמיד — הרשאות, לא קריסה

**נתפס בשטח (27.07.2026), מיד אחרי v0.16.0:** משתמש דיווח ש-`journalctl`
מראה `dmr-dsdfme` יציב לגמרי (בלי restart, בלי קריסה — שגיאות ה-FEC אף
ירדו מ-32 ל-2 לדקה, סימן ששני התיקונים הקודמים עובדים), ובכל זאת **כל**
פקודת AGC/רווח נכשלה עם "dmr-dsdfme רץ?" — גם `agc:off` וגם `agc:on`
בזה-אחר-זה. תג-המצב בממשק המשיך להראות "AGC פעיל" גם אחרי לחיצה על
הכיבוי, מה שחשף שהפקודה מעולם לא הגיעה בכלל.

### Fixed
- **★ ה-control-socket של הרווח נוצר בלי `chmod` — לא היה כתיב למשתמש
  הלא-פריווילגי.** `dmr-dsdfme.service` רץ כ-`root` (גישת SDR);
  `dmr-web.service` (שמשם `/api/gain` שולח) רץ כמשתמש `dmr` הלא-פריווילגי.
  `bind()` לבדו משאיר את קובץ ה-socket בהרשאות לפי umask של התהליך היוצר
  (root) — בדרך כלל `0755`, כלומר ל"אחרים" יש רק read+execute, **בלי
  write**. `sendto()` ממשתמש `dmr` לנתיב הזה נכשל תמיד ב-`EACCES`,
  **בשני הכיוונים באופן זהה** — בדיוק התסמין שנצפה, ובלי שום קריסה
  שתסביר אותו. `_bind_control_socket()` חדש (משותף לחד-ערוצי ול-multi)
  עושה `os.chmod(path, 0o666)` מיד אחרי ה-bind. נבדק ישירות (בדיקת
  הרשאות-קובץ בפועל אחרי הקריאה), + מקרי-קצה (קובץ-stale במקום socket,
  תיקייה שאי-אפשר ליצור). 345 בדיקות (342→345).

## [0.16.0] - סימולציית DMR מקצה-לקצה: תדר-החתך היה רחב פי-2, והספים בממשק היו שגויים

**מנה שנולדה מבדיקה, לא מדיווח.** לפני עדכון החומרה נבנה **מחולל אות DMR
סינתטי** (`tests/dmr_signal.py`: 4FSK תקני, 4800 סמלים/ש', סטיות ±1944/±648 Hz,
עיצוב raised-cosine) שמוזן לשרשרת ה-DSP **האמיתית** ומודד שיעור שגיאות-סמל
(SER). זו הבדיקה הראשונה שמוכיחה שהדמודולטור באמת **מפענח DMR** ולא רק
"מייצר PCM סביר" — בלעדיה באג הדצימציה (v0.14.0) עבר 300+ בדיקות בירוק
בזמן שהתחנה לא פענחה כלום בשטח.

### Fixed
- **★★ תדר-החתך לפני הדיסקרימינטור היה 10 kHz — רחב פי-שניים מהנדרש.**
  זה רוחב-פס של 20 kHz לערוץ DMR של 12.5 kHz (שתופס ~7.7 kHz), כך שכל
  הרץ מעבר לכך הכניס **רק רעש**; ורעש-FM גדל עם התדר, ולכן העודף פוגע
  יותר ממה שיחס-הרוחב מרמז. הורד ל-**6 kHz** (`DEFAULT_CUTOFF_HZ`, ניתן
  לעקיפה ב-`--cutoff-hz`). **נמדד:** ב-SNR=10dB, SER יורד מ-**5.79% ל-0.06%**
  ב-240kHz, ומ-**0.32% ל-0.02%** ב-672kHz — שיפור של פי-16 עד פי-100.
  6 kHz (ולא 5) נבחר לשוליים מול שגיאת-תדר: עד ~1 kHz סטייה הביצועים
  זהים, ומעבר לכך 6k עדיף. אות נקי מפוענח ב-0% שגיאות בכל הערכים שנבדקו,
  כלומר ההצרה אינה מעוותת.
- **★ הספים במד-העוצמה היו שגויים בשני הכיוונים** (נקבעו בהערכה ב-v0.14.0,
  עכשיו מכוילים במדידה):
  - **הרף התחתון היה מסוכן**: ‎−35 dBFS סומן "תקין", אבל בפועל הפענוח קורס
    כבר ב-‎−34 (נמדד: ‎−26 נקי, ‎−31 שוליים, ‎−34 נהרס). הועלה ל-‎−25.
  - **סף החיתוך זעק לשווא פי-500**: `clip_frac > 0.0005` סומן אדום, אבל
    FM הוא מעטפת-קבועה וסובלני — `clip_frac=0.06` עדיין 0% שגיאות, ו-0.33
    עדיין 0.05%. הוחלף בשני ספים: אזהרה מ-0.05, אדום מ-0.25.

### Changed
- **★ ניסוח הכרטיס תוקן בנקודה מהותית: עוצמה איננה איכות-פענוח.** נמדד
  שערוץ ב-‎−1 dBFS נותן SER של 0% ב-SNR אינסופי ו-11% ב-SNR=8dB — **אותה
  קריאה בדיוק במד**. הכרטיס אומר עכשיו במפורש שרק שגיאות ה-CRC/FEC מעידות
  על פענוח, ושהעוצמה עונה רק על "האם כיוון הרווח נכון".

### Added
- `tests/dmr_signal.py` + 7 בדיקות מקצה-לקצה ב-`test_rsp_fm.py`: פענוח נקי
  בחד-ערוצי וב-multi, ערוץ מוסט, chunk לא-מיושר, עמידות לשגיאת-תדר, וקיבוע
  השיפור של תדר-החתך. 342 בדיקות (335→342).

### ממצאים שנמדדו ולא חייבו שינוי-קוד
- **`scaled_taps` אינו נחוץ**: דחיית הערוץ השכן ב-121 taps @672kHz היא כבר
  ‎−53 dB בהיסט 21 kHz. ההשערה ש"דליפה מערוץ שכן" גרמה לכשלי ה-FEC **הופרכה**.
- **דינמיקה של 8 ביט**: ערוץ חלש שורד הפרש של **עד ~30 dB** מול ערוץ חזק
  באותו חלון (36 dB שוליים, 42 dB הרס). מתחת לכך פיצול האשכול אינו קונה כלום.

## [0.15.0] - יצירת מערכות מהממשק + מחוון רוחב-הפס

### Added
- **★ CRUD למערכות ב-UI** — עד כה אפשר היה רק *לערוך* מערכת קיימת; להוסיף
  מערכת חדשה חייב עריכה ידנית של `/var/lib/dmr/systems.json` ב-SSH (פער
  שתועד ב-v0.13.0 כ"אין CRUD למערכות ב-UI"). נוספו **"+ מערכת"**, **🗑 מחיקה**,
  ושדה **שם** (שלא היה ניתן לעריכה כלל). ה-`id` נגזר מחותמת-זמן ולא מהשם, כי
  `_validate_systems` דורש `^[A-Za-z0-9_-]{1,32}$` והשם עשוי להיות עברי לגמרי.
- **★ "📋 הדבק תדרים"** — הדבקת רשימת תדרים חופשית (שורות/פסיקים/רווחים),
  LCN מוקצה לפי הסדר. הדרך המהירה להקים מערכת מרשימה שכבר ביד.
- **★ מחוון פריסה/רוחב-פס** מתחת לעורך: מציג את פריסת התדרים ואת רוחב חלון
  הקליטה הנגזר ממנה (אותה נוסחה כמו `compute_wideband_plan`), ומציע פיצול
  כשהמרווח הגדול ביותר מצדיק זאת. הרקע: SDR אחד קולט **חלון רציף אחד**,
  ורוחבו נקבע מהמרחק בין הערוץ הנמוך לגבוה — לא ממספר הערוצים. חלון רחב
  פוגע דווקא בערוצים החלשים, כי ה-AGC משותף וה-IQ הוא 8 ביט.

### Fixed
- טיוטת-מערכת שלא מולאה מנעה שמירה של **כל** המערכות: `PUT /api/systems`
  מאמת את הסט כולו ודוחה הכל אם אחת פסולה. ה-UI מסנן עכשיו טיוטות
  (`completeSystems`) לפני השמירה.

## [0.14.0] - "יש אות, אין פענוח": באג הדצימציה ב-multi, מד-עוצמה אמיתי, ובקרת AGC

**נתפס בשטח (27.07.2026).** המשתמש דיווח שבמצב רב-ערוצי אין שום שיחה, רק
שגיאות. ה-journal הראה `Sync: +DMR` ואחריו **100% כשלי `CACH/Burst FEC ERR`**
עם `Color Code=XX` — כלומר DSD-FME מוצא אנרגיית DMR אמיתית ולא מצליח לפענח
אף burst. זה קרה גם בערוצים במרחק 531kHz זה מזה, מה שפסל את תיאוריית
"דליפה מערוץ שכן" (ו-`DSD_MULTI_SCALED_TAPS`). ⚠ הקשר חשוב: התחנה הזאת
**מעולם לא פענחה שיחה אמיתית מקצה-לקצה** — v0.4.0 אימת שהשרשרת *עולה*, לא
שהיא *מפענחת*, ו-68 צורות הפיקסצ'ר הגיעו מקליטה חיצונית.

### Fixed
- **★★ פאזת הדצימציה לא נשמרה בין chunks — ה-multi ייצר PCM ב-48,020Hz
  במקום 48,000.** `NfmDemodulator` נושא בקפידה `overlap`/`previous`/
  DC-blocker/`_mix_phase` בין קריאות `process()`, אבל `filtered[::D]` התחיל
  תמיד מאינדקס 0. זה נכון **רק** כשאורך ה-chunk מתחלק ב-`D` — והחד-ערוצי
  עמד בזה **במקרה** (240000/48000=5, ו-chunk=24000 מתחלק ב-5). ב-multi
  ב-672kHz `D=14` ו-24000 % 14 = 4, ולכן בכל גבול-chunk רשת-הדגימה החליקה
  10 דגימות (~15µs מתוך סמל של 208µs) ונפלטה דגימה עודפת — סחיפת קצב של
  +417ppm. סנכרון (24 ביט) עדיין נתפס מדי פעם, אבל ה-FEC על ה-burst המלא
  נכשל **תמיד**. עכשיו הפאזה נישאת בין chunks והקצב מדויק לכל `iq_rate`.
  ⚠ **חד-ערוצי לא הושפע ולא השתנה** (הפאזה נשארת 0). נבדק ברגרסיה.
- **★ נמחק הקבוע המומצא `-50.0`** ש-verb ה-rigctl `l` החזיר (הפרת §8 שהמתינה
  מאז v0.13.0). מחזיר עכשיו מדידה אמיתית, או `RPRT 1` כשאין מדידה.
- **★ טראנקינג נכפה על מערכות חד-ערוציות והזיק להן.** כל מערכת קיבלה
  `-T -C -U`, כך ש-DSD-FME נשלח לרדוף אחרי Rest-LSN שלא קיים, ו-
  `render_channelmap(lsn_pairs=True)` שכפל את הערוץ היחיד לשתי שורות-LSN
  מדומות. עכשיו טראנקינג הוא ברירת-מחדל **רק** ל-≥2 ערוצים
  (`_default_trunk`/`_system_trunks`; שדה `trunk` מפורש עדיין גובר) — מערכת
  חד-ערוצית פשוט מכוונת לתדר ומפענחת, כמו מסרוק.

### Added
- **★ מדידת עוצמה אמיתית (dBFS) — סוף העיוורון.** לא הייתה שום דרך לדעת אם
  הקליטה חלשה, חזקה מדי, או חתוכה; כיוון אנטנה ורווח נעשה בעיוורון מוחלט.
  `iq_level_dbfs()` (טהורה) מודדת את ה-front-end מה-IQ הגולמי —
  `rms_dbfs`/`peak_dbfs` + **`clip_frac`** (שיעור הבתים על מסילת 0/255, מדד
  ה-over-gain האמיתי) — ו-`NfmDemodulator._measure_level` מודדת עוצמה
  **פר-ערוץ** אחרי הפילטר. נחשף ב-verb `LEVEL` (אותו דפוס pull כמו
  `SPECTRUM`) → `/api/rf` (`level`/`level_by_channel`) → כרטיס-UI עם מד,
  אזור-יעד מסומן (‎−35..−15 dBFS) ואבחנה מילולית ("חלש — בדקו אנטנה" /
  "⚠ חיתוך — הורידו רווח"). **כשהגשר לא רץ מוצג "אין מדידה"** — לא מספר מומצא.
  ⚠ יחסי-לסקאלה ואמין להשוואה; **לא** מכויל כ-dBm מוחלט.
- **★ בקרת AGC מפורשת.** `connect()` מדליק AGC, אבל הנוד הראשון-אי-פעם היה
  מפיל אותו לרווח ידני **לצמיתות** — בלי פקודת-חזרה (רק restart מלא לשירות)
  ובלי שהמשתמש ידע שזה קרה. נוספו `agc:on`/`agc:off`/`gain:N` בערוץ-הבקרה,
  `RtlTcpClient.agc`+`gain_state()`, ו-`POST /api/gain` מקבל עכשיו גם
  `{agc}` ו-`{index: 0–28}`. ב-UI: טוגל AGC + מחוון רווח + תג-מצב.
  ⚠ `readback:false` מוצהר במפורש — פרוטוקול rtl_tcp הוא כתיבה-בלבד לרווח,
  אז זה "מה שפקדנו", לא מדידה.

### Fixed (UI)
- ערכים חתומים (`-24.3 dBFS`) רונדרו כ-`24.3-` בדף RTL — סימן המינוס תו
  ניטרלי ונדד לקצה השני. תוקן עם `dir="ltr"` ובידוד FSI/PDI. נתפס באימות
  חזותי (Playwright), לא ב-`node --check`.
- מד-העוצמה השתמש בטוקני-צבע שאינם קיימים (`--ok`/`--warn`/`--err`) ⇒ הפס
  היה שקוף לחלוטין. הוחלף בטוקני-המדידה המאומתים (`--st-good`/
  `--st-critical`/`--amber`). גם זה נתפס רק בצילום-מסך.

## [0.13.2] - תוקן: `_terminate_all` (v0.13.1) הייתה קוד מת — dsd_pty מת מיד על SIGTERM, בלי handler

**נתפס בשטח מיד אחרי 0.13.1:** גם עם `_terminate_all`, `sudo systemctl status
dmr-dsdfme` המשיך להראות `Active: failed (Result: timeout)` אחרי `restart`/
`stop` במצב `multi`. ה-journal חשף את הסיבה: ל-`dsd_pty.py` **אין בכלל
handler ל-SIGTERM** (ולא היה גם קודם) — ברירת המחדל של Python ל-SIGTERM
(כשלא רשמו handler) היא **הריגת התהליך מיידית ע"י המערכת**, בלי להריץ שום
קוד Python — כולל בלי להריץ את ה-`finally:` block. systemd דיווח על ה-Main
PID כ-`code=killed, signal=TERM` **בלי אף שורת-לוג אחת** מהניקוי של dsd_pty
עצמו, בזמן ש-rsp_tcp (שכן רושם handler משלו ב-C, מודפס "Signal caught, ask
for exit!") נשאר בחיים עוד 8 שניות עד ש-systemd הרג אותו בכוח
(`KillMode=control-group`) ודיווח `Result: timeout`. כלומר: `_terminate_all`
מ-0.13.1 היה נכון כקוד, אבל **מעולם לא רץ** — התהליך שמקבל את האות מת לפני
שהגיע ל-`finally:` בכלל.

### Fixed
- **★ `_install_shutdown_handler()` חדש** רושם handler ל-SIGTERM/SIGINT
  שזורק `_ShutdownRequested` (יורש מ-`SystemExit`, לא `Exception` — כדי
  שלא ייתפס ב-`except (OSError, RuntimeError, ValueError)` שעוטף את הלולאה
  הראשית ב-`_run`/`_run_multi`). נקרא כשורה הראשונה של `_run()` (מכסה גם את
  `_run_multi` שהוא מפעיל). עכשיו SIGTERM/SIGINT באמת מפסיקים את
  `select.select()` הפעיל, מגיעים ל-`finally:`, ו-`_terminate_all` (0.13.1)
  סוף-סוף רץ ומזרז את מות rsp_tcp (SIGKILL תוך ~3ש' מהקוד שלנו) **לפני**
  ש-`TimeoutStopSec=8` של systemd פוקע. נבדק (`test_install_shutdown_
  handler_raises_on_sigterm`/`..._sigint_too`) ע"י שליחת האות בפועל לתהליך
  הבדיקה עצמו ווידוא `_ShutdownRequested`, + בדיקה מפורשת שהמחלקה לא נבלעת
  ע"י ה-`except` הקיים.

## [0.13.1] - תוקן: `dmr-dsdfme` (multi) נכשל ב-restart עם `Result: timeout`

**נתפס בשטח (27.07.2026):** משתמש דיווח על מצב רב-ערוצי (6 ערוצים) שלא הפיק
אף שיחה ו"רק שגיאות" — `sudo systemctl status dmr-dsdfme` הראה
`Active: failed (Result: timeout)`, ו-`/api/gain` נכשל עם "dmr-dsdfme רץ?"
(ה-control-socket כבר לא היה שם כי dsd_pty כבר לא רץ).

### Fixed
- **★ עצירת/restart של `dmr-dsdfme` במצב `multi` הייתה עלולה לחרוג מ-
  `TimeoutStopSec=8` של היחידה ולהיכשל.** ה-`finally` block ב-`dsd_pty._run`/
  `_run_multi` סגר את כל תהליכי-הבן **בזה-אחר-זה** דרך `_terminate()`
  (עד 3ש' המתנה ל-SIGTERM + עד 2ש' ל-SIGKILL, **פר תהליך**). ברב-ערוצי זה
  rsp_tcp+rsp_fm.py+N מפענחי DSD-FME — עד 8 תהליכים באשכול 6-ערוצים, כלומר
  עד 40ש' תיאורטית לעצירה מלאה, בעוד `TimeoutStopSec=8` נותן ל-systemd לחכות
  רק 8 שניות לפני ש-`KillMode=control-group` הורג את כל ה-cgroup בכוח ומסמן
  את השירות כ-`failed` (עצירה שנכשלה, לא קריסה). התיקון: `_terminate_all`
  חדש שולח SIGTERM **לכולם קודם**, ואז ממתין על כולם מול **deadline משותף
  אחד** (ואותו דבר ל-SIGKILL) — זמן ההמתנה הכולל נשאר קבוע (~5ש') בלי קשר
  למספר הערוצים. נבדק עם תהליכים מדומים (`test_terminate_all_falls_back_to_
  kill_for_stragglers`) שממחיש בדיוק את המקרה שנצפה בשטח.

## [0.13.0] - כשלים שקטים: שיחות חירום שנעלמו, ארכיון שמאבד נתונים, ו-listener שמת מדאטהגרם אחד

**המנה הזאת לא הוסיפה יכולת — היא החזירה יכולות שהיו אמורות לעבוד ולא עבדו.**
ארבעה סוכני-בדיקה עצמאיים נשלחו לטעון עמדות מנוגדות על "מה הפיצ'ר הבא",
ושלושה מהם הגיעו בנפרד לאותה מסקנה: לפני שמוסיפים משהו, יש כאן קבוצת באגים
שמשמידים נתונים **בשקט** — בלי שגיאה, בלי לוג, ובלי שום סימן בממשק. כל
הארבעה שוחזרו בהרצה אמיתית לפני התיקון, וכל אחד מהם הוא עכשיו בדיקת-רגרסיה.

### Fixed
- **★ דאטהגרם פגום אחד הרג את הפיד לנצח.** ששת ענפי רדאר-המערכת
  ב-`_dmr_listener` היו היחידים בלופ בלי `try/except` (בזמן ש-`_normalize_dsd`
  שלידם כן היה עטוף), ובתוכם `int()` חשוף על שדה מהשידור. `{"type":
  "site_info","site":"abc"}` בודד ⇒ `ValueError` ⇒ ה-thread מת ⇒ **אפס שיחות
  לנצח**, בזמן ש-`/api/health` ממשיך לדווח `ok=True`, השירותים `active` והפיל
  בממשק אומר "מאזין". שוחזר בהרצה (`listener alive: False`). התיקון:
  הטיפול-בדאטהגרם הופרד ל-`_handle_datagram` שעטוף במלואו, `record_site`/
  `record_lsn_status` מדלגים על קלט לא-מספרי, ונוסף `_listener_watchdog`
  שמקים את ה-thread מחדש אם בכל זאת ימות.
- **★ שיחות חירום לא הגיעו לשום מקום.** `_RE_VOICE_CALL` הרשה רק `TXI` בין
  `Group`/`Private` ל-`Call`, כי 20,000 השורות שנקלטו הכילו רק `SO=0x00` ו-
  `SO=0x20`. אבל `dmr_flco.c` מדפיס שם עד עשרה טוקנים נוספים — ו-8 מתוך 9
  וריאציות הפילו את **הכרטיס כולו**: `Emergency` (so&0x80), `Encrypted`
  (so&0x40), `Priority 1/2/3`, `Broadcast`, `OVCM`, `RPT`, ושמות-וונדור.
  שיחת חירום לא הופיעה בפיד, לא ב-`dmr.jsonl`, לא באנליטיקה ולא ב-watchlist.
  התיקון גוזר את סדר-ההדפסה המדויק מקוד-המקור, ומוסיף `emergency`/`priority`/
  `flags` לכרטיס + תג 🚨 בממשק. `encrypted` נקבע עכשיו **מהמקור** (דגל ה-SO)
  כשהוא נוכח; קורלציית ה-`Protected LC` נשארת fallback ויכולה רק להדליק.
- **★ הארכיון איבד `dur`/`frames`/`encrypted`/`id` — על כל שיחה, תמיד.**
  `_append_dmr_log` נקרא בפריים הראשון, לפני שהמוטציות מצטברות על האובייקט
  החי, ואין writer שני. לכן `/api/analytics/traffic?day=` דיווח `airtime 0.0`
  ו-`encryption?day=` דיווח `0%` מוצפן **לנצח**, וייצוא ה-CSV נתן עמודות
  ריקות בסתירה לתגי 🔒 שעל המסך. עכשיו שיחות-קול נכתבות **בסגירת השיחה**
  (`_close_stale_calls`, `CALL_CLOSE_SEC=20ש'` — גדול משני החלונות שממתתים
  כרטיס: dedup 8ש' וקורלציית-הצפנה 15ש'), וכרטיסי data/lrrp נכתבים מיד כי
  אינם משתנים. טיק תקופתי ב-`_activity_watcher` סוגר שיחות גם כשהתעבורה
  נעצרה, והקמה-מחדש של ה-listener מבצעת flush ולא מאבדת שיחה פתוחה.
  **המחיר המודע והמתועד:** קריסת-listener מאבדת שיחות שטרם נסגרו (≤20ש').
- **★ `systemctl restart dmr-web` השתיק את כל מודיעין-המערכת.** `_boot_restore`
  יוצא מוקדם כש-`live == mode` (המצב הרגיל כשהמפענח כבר רץ), ו-
  `_active_system_id` נקבע **רק** ב-`_enter_dmr` ⇒ `_intel_system_id()` החזיר
  `None` לתמיד וכל v0.11.0+v0.12.0 (אתרים/LSN/CDR/סחיפת-CC/הצבעות מיפוי-LSN)
  צבר אפס, בשקט. נוסף `_restore_intel_cache(state)` על מסלול-היציאה הזה.
- `scripts/spike-dmr-multi` כותב עכשיו גם הוא את שורת-הכותרת של
  `channelmap.csv` (אותה גוצ'ה של v0.12.0 — `csvChanImport` מדלג על שורה 1).

### Added
- **★ הבחנה בין "הרשת שקטה" ל"השרשרת מתה"** — הפער שכל ארבעת הסוכנים סימנו
  כחמור ביותר לתחנת-האזנה, ושלא היה קיים בשום נקודת-API. שלושת המצבים
  רונדרו קודם **זהה** (פיל ירוק, "מאזין"), ו-`errors_per_min: 0` של מד ה-RF
  דו-משמעי בין הטוב ביותר לגרוע ביותר. האות היה זורם ונזרק: ב-Cap+ טלמטריית
  ערוץ-הבקרה (`lsn_status` — ~חצי מהפלט) זורמת רציף גם בלי שיחות.
  - `_feed_tick`/`_feed_snapshot` — מונה חיוניות שנרשם על **כל** דאטהגרם,
    לפני הדיספאץ' ולפני כל פרסור (כך שגם דאטהגרם בעייתי נספר).
  - `_decode_state` (טהורה) — `decoding` / `chain_alive` / `silent` /
    `listener_down` / `standby`. **שמרנית במכוון:** "silent" ולא "שבור", כי
    בחד-ערוצי non-trunk דממה מלאה היא מצב לגיטימי ואין ראיה להאשים (§8).
  - `/api/health` מחזיר `listener_alive`, `feed` (חלון 60ש' לפי טיפוס-אירוע,
    `last_datagram_at`, `last_voice_at`) ו-`decode_state`; כרטיס-בית חדש
    שמרנדר את שלושת המצבים בנפרד עם הסבר ומספרים.
- **★ גלאי-החמצה בפרסר (`voice_miss`) — הלקח האמיתי מבאג ה-Emergency.**
  שורה שנראית כמו שיחה ולא נתפסה ע"י ה-regex המדויק מייצרת עכשיו אירוע-אבחון
  (נבדק **אחרון**, כדי לא לגנוב שורות מתבניות אמיתיות), נספרת, ומופיעה
  ב-`/api/rf` כ-`parser_miss`/`parser_miss_last` + חיווי בממשק. גרסת DSD-FME
  אחרת או וונדור אחר יפיקו טוקן שאיננו מכירים — ואז נדע, במקום לגלות בעוד שנה.
  `handler_errors` נחשף באותו מקום.
- `tests/fixtures/dsdfme_source_shapes.csv` — **פיקסצ'ר שני, מופרד במוצהר.**
  16 צורות שנגזרו מקוד-המקור של DSD-FME עבור מצבים שהרשת שנקלטה לא שידרה,
  כל שורה עם עמודת `provenance` (`source:dmr_flco.c:545`) ובדיקה שאוכפת
  אותה. `capplus_slco_sample.csv` (68 צורות מקליטה אמיתית) נשאר **קודש ולא
  מזוהם**. זו הרחבה מודעת של כלל §7: "לא מנחשים" הופך ל"רק קליטה או
  קוד-מקור, ותמיד עם תיוג" — כשתגיע דגימה אמיתית, היא עוברת לפיקסצ'ר הקליטה.

### Changed
- `_handle_datagram` הופרד מ-`_dmr_listener` וקיבל `ctx` מפורש (dedup/
  slot_open/pending/seen) ⇒ השכבה ה-stateful, שעד כה לא הייתה נבדקת בכלל,
  נבדקת עכשיו ישירות ב-CI בלי socket ובלי thread.
- 302 בדיקות ירוקות (275→302). אומת גם חזותית (Playwright מול `app.py`
  אמיתי): שלושת מצבי הפענוח בנפרד + כרטיס חירום מתויג, לפי §7.

### ⚠ נשאר פתוח (מחוץ לסקופ המנה הזאת)
באגים שאומתו בסבב אך לא תוקנו כאן, בהחלטה מפורשת: נגן ההקלטות ב-UI הוא קוד
מת (`wav` לעולם לא מוצב ⇒ ההבטחה ב-README לא מתקיימת); `/api/positions` ריק
מבנית (קבוצת ה-`src` ב-`_RE_LRRP_POS` לא יכולה להתאים, כי `dmr_pdu.c` מדפיס
SRC רק כשאין lat — והתיעוד תולה את זה בטעות ב"Motorola proprietary"); ה-UI
לא שולח `X-DMR-PIN` אף פעם ⇒ הפעלת ה-PIN המתועד הופכת את התחנה לקריאה-בלבד;
אין CRUD למערכות בממשק; `rsp_fm.py:583` מחזיר `-50.0` מומצא ל-verb `l` של
rigctl; ו-`index.html:1201` טוען אריחי-מפה מ-`tile.openstreetmap.org` — תלות
ענן חיה שסותרת את §1. וריאציות `data_header` (UDT/Short Data) עדיין נדחות.

## [0.12.0] - מיפוי LSN↔תדר אמיתי (סוף לניחוש ה-LCN) + תיקון מפת-הערוצים של DSD-FME

**נקודת-הפתיחה הייתה שאלה של המשתמש:** בקונפיג מסקר-השדה
(`config/systems.survey.json`) כל תדר קיבל LCN רץ (1,2,3...) לפי סדר-התדרים —
"ולא נראה לי שזה באמת ה-LCN שלו". הוא צדק, ובעומק גדול יותר ממה שנראה: לא רק
שהסדר היה ניחוש — **מרחב-המספור עצמו היה שגוי**, ובדרך התגלה שהערוץ הראשון
במפה נזרק בשקט בכל הרצת-טראנקינג מאז ומתמיד.

### Fixed
- **★ באג ותיק: DSD-FME זרק את הערוץ הראשון במפת-הערוצים, תמיד.**
  `csvChanImport` ב-`src/dsd_import.c` של DSD-FME עושה
  `if (row_count == 1) continue; //don't want labels` — הוא מדלג על השורה
  הראשונה **בלי לבדוק אם היא כותרת**. `render_channelmap` שלנו לא כתב כותרת
  מעולם ⇒ LCN 1 (התדר הראשון) לא נטען אף פעם ל-`trunk_chan_map`, ומעקב-שיחה
  אליו נכשל בשקט. עכשיו נכתבת שורת `# LCN,FREQ_HZ` ראשונה. הכותרת מתחילה
  ב-`#` בכוונה — כך גם `dsd_pty.parse_channelmap_hz` וגם
  `rsp_fm.parse_channelmap_hz` (שקוראים את *אותו* קובץ ב-multi) מדלגים עליה.
  אומת מול קוד-המקור של ה-upstream (ענף `audio_work`), לא מול ניחוש.
- **★ מפת הערוצים ל-Cap+ מאונדקסת ב-LSN, לא בערוץ פיזי.** ב-Capacity Plus כל
  תדר נושא **שני** LSN-ים (LSN 1+2 = הערוץ הפיזי הראשון, 3+4 = השני וכן
  הלאה) — אומת גם מול `dmr_csbk.c` (שמאנדקס `trunk_chan_map[LSN]` וגוזר את
  ה-slot מזוגיות ה-LSN) וגם מול תיעוד ה-upstream. עד כה כתבנו שורה אחת לכל
  תדר פיזי, כלומר DSD-FME קרא בכל מעקב-שיחה את התדר של ערוץ **אחר לגמרי**.
  `render_channelmap(lsn_pairs=True)` מרחיב עכשיו ערוץ פיזי n לשתי שורות
  (LSN 2n-1 ו-2n, אותו תדר). ⚠ ההרחבה מתקנת את ה**פורמט**; את ה**סדר** מגלה
  הפיצ'ר הבא. ב-multi ההרחבה **אסורה** ולא מופעלת (שם אותו קובץ הוא רשימת
  הערוצים לדמודולציה — הכפלה הייתה מייצרת מפענחים כפולים), ולכן
  `_enter_dmr` מעביר `lsn_pairs=not multi`.
- `config/systems.survey.json`: `multi_164cluster` הגדיר `color_code: 8` על
  `control: 164.10625`, אבל הסקר מדד על התדר הזה דווקא **CC=10** (CC8 נמדד על
  164.5375). תוקן ל-10 ⇒ רדאר-המערכת מפסיק לדווח "סחיפת-CC" מדומה לצמיתות.
  (שני האשכולות האחרים היו עקביים מלכתחילה.)

### Added
- **★ גילוי אוטומטי של מיפוי LSN↔תדר (`system_intel.record_rest_channel`).**
  זה נעשה אפשרי בלי שורת-קוד חדשה בשרשרת-האותות — הנתון פשוט **נזרק** עד
  היום: טלמטריית CSBK של Cap+ (`lsn_status`/`site_info`/`preamble_csbk`)
  מודיעה איזה LSN הוא ה-Rest, ו-`dsd_pty.tag_event` כבר מחתים **כל** אירוע
  ב-multi ב-`phys_freq_hz` של המפענח שהפיק אותו. וטלמטריית בקרה יכולה להגיע
  **רק** מהערוץ הפיזי שנושא את ה-Rest LSN ⇒ הצירוף הוא ground-truth. הסתמכות
  על תצפית בודדת נמנעת בכוונה: נצבר כהצבעות, ו-`derive_lsn_map` (טהורה)
  מכריעה לפי מכסת-הצבעות (3) ורוב (75%).
- **הסקת ה-LSN השותף:** LSN שלא נצפה כ-Rest לעולם יורש את התדר מהחצי השני של
  הזוג (`source="pair"` מול `source="rest"` — הסקה מסומנת, לא מוסווית
  כתצפית). זוג ששני חצאיו נצפו על תדרים שונים מסומן `pair_conflict` ו**לא**
  מאומץ — לא "מתקנים" הנחה שבורה בשקט.
- `POST /api/system-intel/apply-lsn` + כפתור "אמץ כ-channelmap" בכרטיס
  🔬 מודיעין-מערכת: מחליף את ה-channelmap של המערכת במיפוי שהתגלה, כשה-`lcn`
  הוא סוף-סוף **מספר הערוץ הפיזי האמיתי** (נגזר מה-LSN) ולא אינדקס-שרירותי —
  וזה מה שהופך את `lsn_pairs=True` לנכון-בפועל ולא רק נכון-בפורמט.
  ⚠ זו הפעולה **היחידה** שבה מודיעין-מערכת נוגע ב-`systems.json`, והיא
  יזומה-אנושית במפורש (POST דרך `_guard`, כפתור) — `system_intel` עצמו ממשיך
  לעולם לא לכתוב לקונפיג (CLAUDE.md §5).
- `GET /api/system-intel` מחזיר גם `lsn_map` (מיפוי מוכרע פר-LSN עם
  הצבעות/ביטחון/מקור/ערוץ-פיזי) ו-`lsn_channelmap` (הצורה המוכנה לאימוץ).
  שניהם **נגזרות** מחושבות בזמן-תצוגה, לא state — כך שסף-ההכרעה יכול להשתנות
  בלי מיגרציית-קובץ.
- `system_intel._profile` עושה השלמת-סכימה קדימה: `system_intel.json` שנכתב
  ע"י v0.11.0 (בלי `lsn_freq`) נטען בלי לקרוס.

### Changed
- **CLAUDE.md §8 עודכן: הכלל "מיפוי LCN↔תדר אינו בר-גילוי אוטומטי" לא נכון
  יותר במלואו.** הנימוק שלו — "SDR יחיד לא יכול לצפות בבקרה ובקול בו-זמנית" —
  היה תקף עד multi mode ונשאר תקף בחד-ערוצי; ב-multi יש מפענח לכל ערוץ ולכן
  הבקרה והקול נשמעים יחד. מה ש**כן** נשאר לא-בר-גילוי: LSN שאין עליו תעבורה
  לעולם, וכל מיפוי במצב חד-ערוצי.
- 21 בדיקות חדשות (254→275), כולן ללא חומרה: ההכרעה הטהורה
  (מכסה/רוב/הסקת-זוג/סתירת-זוג), הכותרת + הרחבת-הזוגות + קריאה חוזרת
  בשני הפרסרים שלנו, listener e2e (UDP→`app.py`→`system_intel`) ושכבת ה-HTTP
  של האימוץ. אומת גם חזותית (Playwright מול `app.py` אמיתי, כולל לחיצה על
  כפתור-האימוץ וכתיבה בפועל ל-`systems.json`), לפי §7.

### ⚠ טרם אומת על חומרה
מנגנון-ההצבעות והפרסור נבדקים ב-CI, אבל **תדירות ה-Rest-rotation בשטח** היא
מה שקובע כמה מהמיפוי יתגלה בפועל, וזה נמדד רק על Pi 5 + RSP1B מול תעבורה
חיה. אם הכיסוי יתגלה חלקי, ההרחבה הטבעית היא קורלטור שני: התאמת
occupant-id של `lsn_status` מול ה-TG/יעד של שיחה שנשמעה בו-זמנית על ערוץ
פיזי ידוע (הנתונים לכך כבר זורמים; לא מומש כדי לא להוסיף מקור-אי-ודאות
שני לפני שהראשון נמדד).

## [0.11.0] - רדאר-מערכת: מודיעין מערוץ-הבקרה, מעשיר את בנק-התדרים

**פיצ'ר-דגל, נוגע בעומק הפרוטוקול:** Cap+ הוא רשת-טראנקינג — ערוץ-הבקרה
משדר כל הזמן טלמטריה על **כל המערכת**, לא רק על עצמו (כדי שרדיואים ידעו
לאן לקפוץ). ~4 סוגי-שורות אמיתיות מ-Cap+/SLCO (מאומתות מול `capplus_slco_
sample.csv`, לא ניחוש) נזרקו עד היום כ"housekeeping" — עכשיו הן מעשירות
את בנק-התדרים (`systems.json`) לאורך זמן, **בלי לתפוס אף ערוץ נוסף ובלי
עלות multi-mode**: מפת-תפוסה חיה של כל הערוצים, "פירוט שיחות" (CDR) לשיחות-
יחיד גם בלי לשמוע אותן, שביל-אתרים גס בלי GPS, וזיהוי-סחיפת Color-Code.

### Added
- **4 טיפוסי-אירוע חדשים ב-`dsd_pty.parse_dsd_line` (תמיד פעילים, לא
  emit_status-מותנים כמו sync/channel_status — ר' CLAUDE.md §8):**
  - `lsn_status` — "LSN 05: 223; LSN 06: 64250; LSN 07: Idle; ..." — מפת-
    תפוסה חיה **של כל הערוצים**, מערוץ-הבקרה בלבד. ⚠ עצם-ה-occupant (מספר)
    לא מסווג group/private — שני הסוגים תופסים LSN באותה צורה בקליטה
    האמיתית, לא ממציאים סיווג שלא אומת.
  - `preamble_csbk` — "Preamble CSBK - Individual CSBK/Data - Source: X -
    Target: Y - Rest LSN: N" — הקמת שיחת-יחיד (מי→מי), **גם בלי לשמוע**
    את השיחה בכלל (payload בערוץ אחר).
  - `bank_call` — "Bank One F80 ... - LSN N: TGT M;" — אישור-תפוסה מתמשך
    לשיחות-יחיד (target בלבד, לפעמים רשימה ריקה = אין שיחות פעילות כרגע).
  - `site_info` — "SLCO Capacity Plus Site: N - Rest LSN: M - RS: 0" —
    זהות-האתר (Cap+ רב-אתרי) שהבקרה משדרת ממנו כרגע.
  - כל הארבעה נבדקו מול **כל** הווריאציות האמיתיות בפיקסצ'ר (34 lsn_status
    שונים!, 7 bank_call, 2 preamble_csbk, 1 site_info) — לא רק דוגמה אחת.
- **`webtune/system_intel.py` (מודול חדש):** "הצד השני" של `systems.json` —
  config נשאר סטטי (עורכים ידנית), כאן נצבר **ידע** (`/var/lib/dmr/
  system_intel.json`, קובץ-state נפרד — **לעולם לא נכתב חזרה ל-systems.json
  עצמו**, אין סיכון להחלפה-מלאה של קונפיג-משתמש). `record_site`/
  `record_lsn_status`/`record_private_call`/`record_cc` — כולן טהורות
  (מקבלות ערכים ישירות, נבדקות בלי UDP/Flask). ⚠ `lsn_status` לבדו הוא
  ~חצי מהפלט האמיתי (34/68 בפיקסצ'ר) — כתיבה-לדיסק על כל אירוע הייתה
  שוחקת כרטיס-SD; `maybe_flush()` debounced (ברירת מחדל 15ש') + thread-
  גיבוי תקופתי (`_intel_flush_watcher`) מבטיח שמירה גם כשהתעבורה נרגעת.
- **זיהוי-סחיפת Color-Code:** `cc` שכבר מפוענח על אירועי `quality` (ונזרק
  עד היום) מושווה מול ה-`color_code` המוגדר במערכת (`record_cc`) —
  `mismatch=True` יכול לרמז על קונפיג שגוי או הדלפת-אות ממערכת שכנה.
- **`_active_system_id`/`_active_color_code` (מטמון בזיכרון, נקבע ב-
  `_enter_dmr`):** ה-listener לא קורא `load_state()`/`load_systems()`
  (דיסק) על כל אירוע UDP — `lsn_status` תכוף מדי לזה.
- **`GET /api/system-intel`** (`?system=<id>` לצפייה במערכת ספציפית,
  ברירת מחדל: הפעילה) — אין PUT, זה לא config נערך-ידנית.
- **UI: כרטיס "🔬 מודיעין-מערכת"** — סטטוס-CC (מוגדר מול נצפה + התראה
  על אי-התאמה), אתרים שנצפו, מפת-תפוסה פר-LSN, ורשימת שיחות-יחיד אחרונות
  (CDR). אומת חזותית (Playwright, מוק-נתונים כולל סחיפת-CC מכוונת).

### Verification status
מוצא מ**דגימות אמיתיות** (לא ניחוש) — 44/44 מהפיקסצ'ר (68 השורות) עוברות
דרך `test_fixture_replay_matches_reality`. **שרשרת ה-UDP המלאה (dsd_pty→
app.py→system_intel) נבדקה end-to-end** (254 בדיקות ירוקות, 220→254) אבל
**עדיין לא אומתה על חומרה אמיתית** — כמו כל שינוי ב-`dsd_pty.parse_dsd_line`
(CLAUDE.md §8), ותדירות-האמת של הטיפוסים החדשים (עד כמה `lsn_status`/וכו'
משתנים בפועל מעבר ל-68 הדגימות) היא בדיוק סוג-המידע שרק ריצה בשטח נותנת —
אם מגיעה דגימה אמיתית חדשה שלא תואמת, מוסיפים לפיקסצ'ר, לא ממציאים.

## [0.10.0] - מעקב RID/TG עם התראה מקומית (בלי Web Push/ענן)

פיצ'ר-תפעול ללא-חומרה: לדעת מיד כשמישהו במעקב מדבר, בלי לבהות בפיד.

**⚠ החלטת-ארכיטקטורה:** Web Push API הסטנדרטי (גם self-hosted) עובר תמיד
דרך שרת-relay של ספק-הדפדפן (Google FCM / Mozilla Autopush / Apple) —
זו לא בחירת-מימוש אלא איך ה-API בנוי. זה סוטה מ"פרטי-מקומי (בלי ענן)"
שב-CLAUDE.md §1. **נבחר במפורש שלא** ללכת בכיוון הזה. במקום זה: התראה
**מקומית בלבד** בדף/PWA פתוח — `Notification` API (לא Push API, אין
subscription/VAPID/שרת-relay), רטט (`navigator.vibrate`), וצליל (Web Audio
oscillator, בלי קובץ-מדיה) — כל השרשרת נשארת בתוך הדפדפן/הרשת המקומית.
מגבלה מודעת: לא יעבוד אם ה-PWA סגור לגמרי/הטלפון נעול לאורך זמן (בניגוד
ל-Push אמיתי) — משתלם ב"בלי ענן" באמת, לא רק בהצהרה.

### Added
- **`webtune/watchlist.py` (מודול חדש, מראה `aliases.py`):** רשימת RID/TG
  במעקב, נערכת מהטלפון. `match(tg, src, tgt)` טהורה — מחזירה את ההתאמה
  הראשונה (סדר-עדיפות: tg, אז src, אז tgt; תג יחיד לכרטיס). `GET/PUT
  /api/watchlist` (דרך `_guard`, כמו `/api/aliases`).
- **`_normalize_dsd` מתייג כל כרטיס** עם `watchlist: {kind,id}|None` —
  באותה נקודה שבה `aliasdb` מפענח שמות (אותו עקרון: העשרת-מזהים בזמן
  נרמול, לא בזמן-תצוגה). נשמר גם ב-`dmr.jsonl` (בונוס: עתידי ייצוא/ניתוח
  יכולים לראות אילו שיחות היו במעקב).
- **UI:** כרטיס "🔔 מעקב" (הוספה/הסרה + כפתור "הפעל התראות"), תג `🔔ᴹ`
  על כרטיסי-שיחה תואמים, `toast` צף + רטט + צליל + `Notification` (אם
  ניתנה הרשאה) בכל poll שמזהה כרטיס-חדש-תואם.
  **⚠ תוקן בבדיקה-חזותית (לא code review):** ה-batch הראשון של `pollFeed`
  (`since=0`, פתיחת-דף) הוא היסטוריית-היום שכבר קרתה — בלי טיפול מיוחד
  זה הציף בהתראות על שיחות ישנות בכל פתיחה/רענון. תוקן: `wlSeeded`
  מסמן את ה-batch הראשון כ"נראה" בלי להתריע; מתאפס גם בזיהוי
  server-restart (שם ה-ids מתחילים מ-1 מחדש).

220 בדיקות ירוקות (209→220).

## [0.9.1] - restart פר-ערוץ אומת על חומרה אמיתית (25.07.2026)

**✅ מנגנון ה-restart-פר-ערוץ מ-v0.9.0 אומת בפועל** על Pi 5 + RSP1B (multi
mode דרך `dmr-web`, לא ספייק — כלומר גם `app.py`/`/api/rf`/ה-UI נבדקו
end-to-end, לא רק `dsd_pty.py`):

- `sudo kill -9` על תהליכי-בן `dsd-fme` בודדים (lcn=6 ואז lcn=5, מתוך 6
  ערוצים חיים ב-`multi_164cluster`) → journalctl הראה
  `dsd_pty: dsd-fme for lcn=N exited -- respawning (attempt 1/3)` לכל אחד,
  ושני המפענחים קמו-מחדש **על אותו audio port** והתחברו-מחדש בהצלחה
  (`TCP Connection Success!`; `rsp_fm` קיבל לקוח חדש בלי בעיה).
- `/api/rf`'s `by_channel` דיווח `status=restarting, restart_count=1` לשני
  הערוצים, בדיוק כצפוי.
- **`systemctl status dmr-dsdfme` נשאר `active (running)` ללא הפרעה** לאורך
  כל הבדיקה — ה-service *לא* עבר restart, בדיוק המטרה (בניגוד להתנהגות
  שלפני v0.9.0 שהייתה מפילה את כל השירות). 4 הערוצים האחרים המשיכו לפענח
  ללא שינוי לכל אורך הבדיקה.

מסלול-הוויתור (חריגה ממכסת-restart) עדיין לא נבדק בפועל בשטח — רק
`_channel_restart_decision` (טהורה) ב-CI.

## [0.9.0] - restart פר-ערוץ ב-multi (Phase 7): מפענח בודד לא מפיל הכל

פער-יציבות שנתפס בסקירה: `dsd_pty._run_multi` הפיל את **כל** שירות ה-multi
אם מפענח DSD-FME **בודד** קרס — ל-6 ערוצים זה מגדיל את שטח-הפגיעה במקום
לבודד תקלות. תוקן: מפענח שקורס קם-מחדש **במקומו** (אותו audio port —
`rsp_fm`'s AudioServer כבר סובל חיבור-לקוח חדש, כמו כל ניתוק-DSD-FME רגיל),
בלי לגעת ב-`rsp_tcp`/`rsp_fm`/שאר המפענחים. ערוץ שממשיך לקרוס מוותרים עליו
אחרי מכסת-restart, **בגלוי** — לא נעלם בשקט.

### Added
- **`dsd_pty._channel_restart_decision` (טהור) + `build_decoder_status_event`
  (טהור)** — מדיניות respawn/give-up (עד 3 ניסיונות ב-5 דקות פר-ערוץ, נגזם
  לפי חלון-זמן) ובניית אירוע `decoder_status` (סוג חדש, נבדל מ-`channel_status`
  של DSD-FME עצמו). `_run_multi`: מפענח שמת נקם-מחדש עם אותו command/port;
  אם חרג מהמכסה — הערוץ מוסר מ-`dsd_procs` וממשיכים בלעדיו; אם *כל* הערוצים
  ויתרו — כשל כל השירות (כמו קודם, אין טעם להחזיק rsp_tcp/rsp_fm בלי מפענחים).
  `pragma: no cover` כמו שאר `_run_multi` — הלוגיקה הטהורה נבדקת ב-CI, ה-
  orchestration בפועל דורש אימות על RSP1B בחלון-החומרה הבא.
- **`app._channel_status_tick` + הרחבת `_rf_quality_by_channel`** — ערוץ
  שדיווח `decoder_status` אי-פעם מופיע ב-`/api/rf`'s `by_channel` עם
  `status`/`restart_count`, **גם אם הפסיק לייצר טיקי-RF** (לא נעלם בשקט —
  CLAUDE.md: לעולם לא מסתירים מדד). מתאפס ב-`_enter_dmr` (restart אמיתי).
- **תג-סטטוס בלוח-הערוצים (UI):** 🟡 "קם-מחדש (N)" / 🔴 "נפל (N× ניסיון)"
  לצד תדירות-השגיאות הרגילה. אומת חזותית (Playwright, מוק-נתונים מעורבים).

209 בדיקות ירוקות (198→209).

## [0.8.0] - תור לא-מזוהים: מתן-שם מהיר ל-RID/TG שנצפו בתעבורה

פיצ'ר-מודיעין ללא-חומרה: הופך מספרים גולמיים למשמעות. עד היום כרטיס אליאס
נוצר רק אם ידעת מראש איזה RID/TG למלא; עכשיו התחנה מציגה **worklist** של כל
מי שדיבר אך עדיין בלי שם, ממוין לפי פעילות — click-to-name.

### Added
- **`_unknown_aliases(recs)` (טהור) + `GET /api/aliases/unknown`** — אוסף RID
  (source + target) ו-TG שנצפו בתעבורה, מסלל החוצה כל מה ש-aliasdb פותר **כרגע**
  (ייבוא/ידני), ומחזיר את השאר עם count + last_t + הקשר (אילו TG-ים דיבר RID /
  כמה רדיו על TG), ממוין לפי count יורד. אותם פרמטרים כמו האנליטיקה
  (`?day=`/`?all=1`/היום). מתן שם מפיל את הרשומה מהתור בקריאה הבאה.
- **פאנל "לא-מזוהים" בכרטיס האליאסים ב-UI** — שורה לכל ID עם קלט-שם מוטבע
  + "שמור" (Enter עובד), הקשר (TG-ים/מספר רדיו), ספירה וזמן-אחרון. כפתור "רענן".
  מתרענן אוטומטית אחרי כל מתן/מחיקת שם.

## [0.7.4] - תיקוני ספייק: דגל scaled עמיד-sudo + המתנה לשחרור SDR מיתום

מהרצת A/B בשדה — שני באגים בספייק (לא בקוד הליבה).

**✅ תוצאת ה-A/B (24.07, Pi 5 + RSP1B):** scaled_taps (339 taps) → CPU 131%
ממוצע/**139% שיא** מול הבסיס (121 taps) → 141%/**148% שיא** מתוך 400%. כלומר
**עלות ה-CPU של הפילטר הרחב זניחה** (אף נמוכה יותר, בתוך רעש) — הקונבולוציה
אינה צוואר-הבקבוק. החשש מרוויית-CPU הופרך. **שיפור-הפענוח עדיין לא נמדד** —
כל ההרצות היו בחלון דומם (0–2 אירועים); צריך חלון-תעבורה עמוס לפני שהופכים
את scaled_taps לברירת-מחדל.

### Fixed
- **`DSD_MULTI_SCALED_TAPS=1 sudo bash …` לא הפעיל את הדגל** — sudo מנקה
  משתני-סביבה, אז הסקריפט רץ עם scaled כבוי (הדפיס "כבוי" למרות הדגל). עכשיו
  הדגל הוא **ארגומנט-מיקום**: `sudo bash scripts/spike-dmr-multi <sys> <sec>
  scaled` (ארגומנטים עוברים דרך sudo). משתנה-הסביבה עדיין עובד כ-fallback אם
  שרד. תוקן גם התיעוד ב-CLAUDE.md §8 (הפקודה שם הייתה שגויה).
- **מוות ב-t≈9s אחרי הריגת יתום** — הסוויפ תפס `rsp_tcp` יתום מהריצה הקודמת
  והרג אותו ב-SIGKILL, אבל ה-SDRplay API משחרר את ה-RSP1B תוך כמה שניות (לא
  מיידית), וההמתנה של שנייה אחת הספיקה בקושי → המנוע הבא נפל ב-bring-up. הועלה
  ל-4ש'. ניקוי-הסיום של הספייק גם שודרג ל-`pkill -9 -f` (כולל `rsp_fm.py`
  שרץ כ-python3) כדי לא להשאיר יתומים מלכתחילה.

## [0.7.3] - scaled_taps הפך ל-opt-in (ברירת-מחדל = המנוע שאומת) לפני מיזוג

החלטת-מיזוג: v0.7.2 הכניס את `scaled_taps` כברירת-מחדל ב-multi — אבל זה
שינוי-DSP שמכפיל ×~2.8 את עומס-הקונבולוציה על תהליך `rsp_fm.run_multi`
היחיד, **ולא אומת על חומרה** (חומרה רחוקה שבועות). CLAUDE.md §8 אוסר למזג
שינוי-שרשרת-אותות על סמך pytest ירוק בלבד. הפתרון: להשאיר את התיקון ב-main
אך **כבוי כברירת-מחדל**, כך שברירת-המחדל של multi נשארת בדיוק המנוע שאומת
ב-v0.7.1 (121 taps קבוע), ושינוי ה-DSP נבדק כ-A/B בשדה.

### Changed
- **`scaled_taps` ב-multi עכשיו opt-in דרך `DSD_MULTI_SCALED_TAPS`** (כבוי
  כברירת-מחדל). `MultiChannelBridge` מזין 121 taps קבוע אלא אם הדגל דלוק;
  `NfmDemodulator` חזר להשתמש ב-`taps` כפי-שהוא (לא סורק פנימית). חד-ערוצי
  (240kHz) זהה byte-for-byte בכל מקרה (`scaled_taps(240k)==121`). הספייק
  (`scripts/spike-dmr-multi`) מדפיס אם הדגל דלוק ומתעד הרצת A/B (עם/בלי).
  **ברגע ש-A/B בשדה יוכיח שיפור-פענוח בלי רוויית-CPU — הופכים לברירת-מחדל.**

## [0.7.2] - תיקוני-multi מסקירה מקיפה: סלקטיביות פילטר + הפרדת-תדרים

שני ממצאים מסקירת-קוד מעמיקה של מסלול ה-multi (לפני מיזוג הרחבה-לשטח).
שניהם נוגעים **רק ל-multi**; חד-ערוצי (dmr/scan) נשאר byte-for-byte זהה
למסלול שאומת על חומרה ב-Phase 5. 192 בדיקות ירוקות (187→192).

### Fixed
- **פילטר anti-alias קבוע (121 taps) לא התאים לקליטה רחבת-פס של multi.**
  `NfmDemodulator` בנה תמיד 121 taps בלי קשר ל-iq_rate. רוחב-המעבר של פילטר
  windowed-sinc הוא ~3.3·fs/taps — כך שב-240kHz (חד-ערוצי) קיבלנו ~6.5kHz, אבל
  ב-672kHz (multi_164cluster) אותם 121 taps התנפחו ל-~18kHz, והערוץ הצמוד
  ב-Cap+ (12.5–25kHz הצידה) דלף פנימה ופגע בפענוח פר-ערוץ — חשוד מרכזי מאחורי
  "רק 2/6 ערוצים נעלו" בספייק. נוסף `rsp_fm.scaled_taps()` (טהור, נבדק ב-CI)
  שמתאים את מספר ה-taps ל-iq_rate כדי לשמור על רוחב-המעבר קבוע (~339 taps @
  672kHz). **ב-240kHz מחזיר 121 בדיוק** ⇒ חד-ערוצי לא משתנה כלל. ⚠ עלות ה-CPU
  ב-multi תימדד מחדש ב-`scripts/spike-dmr-multi` (יש headroom: 154%/400% ב-6
  ערוצים) — שינוי-DSP, דורש אישור על חומרה לפני שנחשב "מוכן-לשטח".
- **`_validate_multi_feasible` לא בדק הפרדת-תדרים.** שני ערוצים באותו תדר (או
  קרובים מ-raster ה-DMR 12.5kHz) היו מריצים שני מדמודלטורי-NFM חופפים (offset
  זהה) על אותו אות — אירועים כפולים בלי הפרדה אמיתית. נוסף `MULTI_MIN_SEP_HZ`
  (12.5kHz) ובדיקה שדוחה ב-400 עם הודעה ברורה. כל שלושת אשכולות הסקר (≥25kHz)
  עוברים; רק כפילות/טעות-הזנה נדחית.

## [0.7.1] - מנוע ה-multi אומת על חומרה + הקשחת ספייק נגד יתומים

**מצב `multi` הורץ בהצלחה על Pi 5 + RSP1B אמיתי** (מערכת `multi_164cluster`,
6 ערוצים, 120ש') — הסיכון הטכני המרכזי הפתוח מ-Phase 7 נסגר.

### אומת על חומרה (spike-dmr-multi)
- **rsp_tcp שרד קליטה רחבת-פס 672kHz** (span 618.75kHz + guard, פי ~2.8
  מ-240kHz החד-ערוצי) — זה היה החשש המרכזי (ר' CLAUDE.md §10 Phase 7).
- **6 מפענחי dsd-fme במקביל** נשארו חיים את כל 120 השניות.
- **CPU נוח:** 133% ממוצע / 154% שיא מתוך 400% (המון headroom).
- **תיוג phys_lcn עובד** — 1318 אירועים הגיעו מתויגים ב-UDP (phys_lcn 3/4).
- **סייג כן:** רק 2/6 ערוצים ייצרו אירועים בחלון הזה, ורובם `quality`/
  `encryption` (לא `voice_call`) — כיסוי-קול על *כל* הערוצים דורש חלון-שדה
  ארוך יותר. המנוע הוכח; שאלת התעבורה-בזמן נשארת לשטח.

### Fixed
- **`scripts/spike-dmr-multi`: טאטוא-יתומים ב-preflight.** ריצה שנקטעה
  (Ctrl+C/קריסה חלקית) השאירה `dsd_pty`/`rsp_tcp`/`rsp_fm`/`dsd-fme` חיים
  מחזיקים את ה-SDR, והריצה הבאה מתה ב-bring-up (`t≈9s`, 0 אירועים) כי
  rsp_tcp לא יכול לפתוח SDR תפוס — בדיוק כלל "SDR אחד בהחלפה" (§8). זה עלה
  שתי ריצות-סרק לפני שאובחן. עכשיו הספייק הורג יתומים (full-cmdline, כי
  rsp_fm/dsd_pty רצים כ-python3) לפני שהוא מתחיל.

## [0.7.0] - שילוב UI מלא למצב multi (Phase A) + תיקוני-UI קטנים

מ-בדיקה חזותית אמיתית (Playwright, לפי CLAUDE.md §7) של ה-UI מול נתונים
מדומים ב-multi — נמצאו ותוקנו 3 באגים אמיתיים (לא רק שיפורי-נראות) + נבנתה
תצוגה ייעודית פר-ערוץ שהמשתמש ביקש מפורשות. `multi` היה קיים ב-backend
מ-v0.6.0/v0.6.2 אבל ה-UI כמעט לא ידע להציג אותו (label שגוי, פיל-בריאות
אדום-שגוי, בלי לוח-ערוצים) — זה הפער שהפאזה הזו סוגרת.

### Fixed
- **`api_health()` קרס `multi` בחזרה ל-"dmr" (אותו דפוס-באג כמו ב-`_live_mode()`
  שתוקן קודם, ב-פונקציה נפרדת עם לוגיקה משוכפלת).** `dmr`/`multi` חולקות
  יחידת systemd אחת, כך ש-`systemctl is-active` לבדו לא יכול להבדיל ביניהן —
  `mode = "dmr" if dmr_active else saved` דרס כל `multi` בחזרה ל-`"dmr"` ברגע
  שהשירות פעיל. תוקן להשתמש ב-`MODE_SERVICE` כמקור-אמת (כמו `_live_mode`).
  נתפס ע"י צילום-מסך אמיתי שהראה פיל "תקלה" אדום במצב multi תקין.
- **`/api/dmr/export` התעלם מ-`?day=`** — לחיצה על ייצוא CSV בזמן צפייה בארכיון
  של יום מסוים ייצאה בפועל את *כל* ה-`dmr.jsonl`, לא רק את היום המוצג. עכשיו
  מסנן לפי `_day_bounds` בדיוק כמו `/api/dmr?day=` (בלי הפרמטר — ללא שינוי).
- **ערכת-נושא (theme toggle) חדשה עם רינדור חצי-כהה/חצי-בהיר שבור.**
  `:root[data-theme="dark"]`/`["light"]` (קוד קיים, לא-פעיל בפועל עד עכשיו כי
  שום דבר לא קבע `data-theme`) דרסו רק 3 מתוך ~10 המשתנים ש-`@media
  (prefers-color-scheme)` דורס — הרחבת הטוגל חשפה שהשאר ("יתומים") נשארו
  בערך הישן. תוקן: שני הבלוקים דורסים עכשיו את כל 10 המשתנים. גם תוקן
  הלוגיקה ב-JS: זיהוי "מצב נוכחי" באמצעות `matchMedia` בכל לחיצה במקום משתנה
  JS יציב — לא תאם תמיד את ברירת-המחדל האמיתית של הקאסקייד (כהה, לא "בהיר
  כשה-media-query הכהה לא תואם"). אומת חזותית: שני הכיוונים מרנדרים עכשיו
  עקבי לגמרי (Playwright, בדיקת פיקסלים ישירה — לא רק עין).

### Added
- **לוח-ערוצים ייעודי ב-multi (`renderChannelsBoard`) — שורה אחת לכל ערוץ**
  בבית: LCN+תדר, קריאה אחרונה (tg/alias, זמן), תדירות-שגיאות RF פר-ערוץ
  (`by_channel`, אף פעם לא dBFS/SNR). שורה לחיצה → קופצת לפיד השיחות מסונן
  לאותו ערוץ. זה מה שהמשתמש ביקש מפורשות (מעבר להצגת תדר על כרטיס שיחה בלבד).
- **תג `LCNn` על כרטיסי שיחה** (`phys_lcn`, רק ב-multi) + סינון `lcn:N`/`lcnN`
  בשדה החיפוש בפיד.
- **כרטיס-מצב "📡 רב-ערוצי"** בבית + כפתור "התחל רב-ערוצי" — כבוי כשלמערכת
  הנבחרת אין ≥2 ערוצי channelmap (אותו תנאי כמו `_validate_multi_feasible`).
- **`modeLabel()`/כותרת-משנה בבית** כוללים עכשיו ענף אמיתי ל-`multi`
  ("רב-ערוצי · N ערוצים בו-זמנית · {system}") — לפני כן נפלו לברירת-המחדל.
- **טוגל ערכת-נושא (🌓)** בסרגל-העליון + שמירה ל-`localStorage`.
- **כרטיס מתח/טמפ'** (`/api/power`, שכבר היה קיים ב-backend) — פיל אדום
  ב-undervolt/throttle *עכשיו*, כתום אם קרה בעבר, ירוק אחרת.

### בדיקות
- 187 בדיקות ירוקות (184→187): `test_api_health_multi`,
  `test_api_dmr_export_day_filter`, `test_api_dmr_export_bad_day`.
- UI: `node --check` על ה-JS המחולץ + אימות חזותי מלא (Playwright, מוק-שרת
  שמריץ את `app.py` האמיתי עם 19 מערכות הסקר + מצב multi מדומה) — צילומי-מסך
  בית/שיחות/כהה/בהיר, כולל בדיקת-פיקסלים ישירה לוודא עקביות ערכת-הנושא.

## [0.6.2] - תיקוני מצב multi (לפני אימות חומרה) + ספייק ייעודי ל-DMR

מ-code review מעמיק (3 סוכנים) לפני ההרצה הראשונה של multi על חומרה. כל
התיקונים נוגעים אך ורק במצב multi — חד-ערוצי (dmr/scan/discover) לא נגע.

### Fixed
- **#1 (גבוה) — הקלטות multi גרמו למילוי-דיסק בלי גבול והיו בלתי-נראות ב-UI.**
  DSD-FME כותב per-call WAV לתת-תיקיות פר-ערוץ (`recordings/lcnN/`), אבל כל
  ניהול-ההקלטות השתמש ב-`glob("*.wav")` לא-רקורסיבי → ה-retention (400 קבצים)
  לא ראה אותן, הן לא הופיעו ב-`/api/activity`, ולא ניתנו להגשה. תוקן: `rglob`
  בשלושת המקומות (retention/activity/תמלול), `_scan_new_recordings` שומר נתיב
  יחסי (`lcnN/foo.wav`), ו-`/recordings/<path:name>` מגיש תת-נתיבים.
- **#2 (בינוני) — `/api/gain` (נוד-רווח חי) היה מת ב-multi.** `_run_multi` לא
  פתח את `dsd-ctrl.sock` שה-`_run` החד-ערוצי פותח, אז הבקשה החזירה 500. נוסף
  ערוץ הבקרה (bind + select + forward ל-`DSD_BRIDGE_CTRL_SOCK`), זהה ל-`_run`.
- **#3 (נמוך) — `compute_wideband_plan` יכל להחזיר `iq_rate` מעל תקרת ה-2MHz.**
  בדיקת-התקרה הייתה על `span+guard` הגולמי *לפני* העיגול לכפולה של 48kHz, אז
  span שנכנס בקושי (1.99MHz) עוגל מעל התקרה (2.016MHz) ועבר בטעות. תוקן בשני
  העותקים (dsd_pty + rsp_fm): הבדיקה עכשיו על ה-iq_rate המעוגל.
- **#4 (נמוך) — LCN כפול ב-channelmap גרם לכשל bring-up אטום ב-multi.**
  `MultiChannelBridge` ממפתח מדמודלטורים לפי LCN → LCN כפול היה מפיל דמודלטור
  בשקט. `_validate_multi_feasible` דוחה עכשיו LCN כפול ב-400 עם הודעה ברורה
  (ממוקד ל-multi בלבד — לא מהדק את `_validate_systems` הגלובלי).
- **#5 (קוסמטי) — סף ניקוי `_slot_open_call`** הוגדל מ-8 ל-`2×MULTI_CHANNELS_MAX`
  (עד 2 slots × N ערוצים ב-multi), עם תיקון ההערה המטעה.

### Added
- **`scripts/spike-dmr-multi`** — ספייק חומרה שבודק את מנוע ה-multi של **DMR
  עצמו** (`dsd_pty._run_multi` + `rsp_fm.run_multi` + `rsp_tcp` רחב-פס), **לא**
  את ה-channelizer של DECREP (ש-`spike_multichannel.sh` בריפו DECREP בודק —
  נתיב DSP שונה לגמרי). מריץ את השרשרת ש-`/api/mode {"mode":"multi"}` מרים,
  ישירות ובמבודד, ומודד: שרידות `rsp_tcp` ברוחב-פס (הסיכון #1), נעילת-sync
  פר-ערוץ (אירועים מתויגי-`phys_lcn` ב-UDP), ו-CPU. זה האימות שהיה חסר.

### בדיקות
- 184 בדיקות ירוקות (177→184): הקלטות בתת-תיקיות, גבול תקרת wideband-plan,
  ו-LCN כפול. 68/68 fixture replay נשאר ירוק ללא שינוי.

## [0.6.1] - סקר-שדה אמיתי + בעלות פורטים סופית (8080/8081)

### Added
- **`config/systems.survey.json`** — 19 מערכות DMR אמיתיות ממדידת-שדה עצמאית
  (17.07.2026, VHF 162–167MHz): 16 ערוצים מאומתים (SoapySDR+SDRconnect,
  decoder+SQLite עם `integrity_check` תקין) + 3 מערכות-אשכול ל-`multi` mode.
  אומת בפועל מול `_validate_systems`/`_validate_multi_feasible`. לא נטען
  אוטומטית — `cp` ידני אל `/var/lib/dmr/systems.json`. ר' CLAUDE.md §3/§10.

### Fixed
- **בעלות פורט 8080 סוכמה סופית** בין שני הריפואים שרצים על אותו Pi:
  `dmr-web.service` נשאר 8080 (קבוע, `app.run(port=8080)` — אין מה לשנות, זה
  משטח-הבקרה שעולה תמיד ב-boot). `DMR-DECREP-SHAHAR` (ריפו-רפרנס למיזוג הזה)
  שינה את ברירת-המחדל שלו מ-8080 ל-**8081** ב-`backend/cli.py`/
  `scripts/dmr-monitor.service`/`README.md` (שם, v0.26.4) — כך שהרצה שלו לעולם
  לא תתנגש עם DMR גם בלי `--port` מפורש.

## [0.6.0] - מצב `multi`: פענוח רב-ערוצי בו-זמנית (מיזוג עם DMR-DECREP-SHAHAR)

### Added — מצב `multi`: כל ערוצי ה-channelmap בבת אחת, לא רק תדר-בקרה
מצב `app_mode` חדש (`dmr`/`off`/`scan`/`discover`/**`multi`**) שמפענח **את כל
ערוצי ה-channelmap של מערכת בו-זמנית** — לא רק את ערוץ הבקרה עם מעקב-טראנקינג
כמו `dmr`. תוצאה של מיזוג ארכיטקטורה עם `DMR-DECREP-SHAHAR` (שני פרויקטים
שוכפלו מאותו scaffold): המנוע הרב-ערוצי (channelizer דיגיטלי על קליטת IQ
רחבת-פס אחת) נשאב משם ונשתל לתוך המודל החד-SDR/PWA-עברי המוקשח של DMR.

- **`webtune/rsp_fm.py`:** `NfmDemodulator` קיבל `offset_hz` (ברירת מחדל 0.0,
  זהה byte-for-byte להתנהגות הקודמת) — מיקסר מרוכב שמסיט ערוץ מוסט מהמרכז
  המכוון חזרה ל-DC לפני שרשרת הפילטר/דצימציה/דיסקרימינטור-FM/DC-blocker
  הקיימת, בלי שינוי. `compute_wideband_plan` (טהורה): מרכז+קצב-IQ לקליטה
  רחבת-פס אחת שמכסה כל הערוצים, מעוגל **כלפי מעלה לכפולה של 48kHz** (אילוץ
  `NfmDemodulator`, לא היה מובטח בנוסחה נאיבית). `parse_channelmap_hz`,
  `MultiChannelConfig`/`MultiChannelBridge`/`run_multi` (N מדמודלטורים על
  קריאת-IQ אחת, N שרתי-אודיו). CLI: `--multi-channelmap`/`--audio-tcp-base`.
- **`webtune/dsd_pty.py`:** עותק stdlib-בלבד של `compute_wideband_plan`/
  `parse_channelmap_hz` (dsd_pty נשאר בלי תלות ב-numpy) — **חישוב יחיד** לפני
  spawn, מוזרם במפורש (`--frequency`/`--iq-rate`) גם ל-`rsp_tcp` וגם ל-
  `rsp_fm.py` (שני תהליכי-בן נפרדים שחייבים להסכים על אותו center/rate; לא
  נסמכים על שני חישובים עצמאיים). `build_multi_rsp_tcp_command`/
  `build_multi_bridge_command`/`build_channel_dsd_command` (בלי `-T`/`-U` —
  אין retune פר-ערוץ, LO משותף יחיד), `tag_event` (מתייג כל אירוע עם
  `phys_lcn`/`phys_freq_hz` אמיתיים — ground-truth מ-spawn, לא ניחוש טקסט),
  `_run_multi` (N מפענחי DSD-FME תחת PTY, כשל בכל אחד ⇒ כשל כל השירות, כמו
  חד-ערוצי; רסטרט חלקי פר-ערוץ נדחה). `_run()` מפצל ל-`_run_multi` לפי
  `DSD_MULTI=1`.
- **`webtune/app.py`:** `MULTI_GUARD_HZ`/`MULTI_MAX_SPAN_HZ`/`MULTI_CHANNELS_MAX`,
  `_validate_multi_feasible` (≥2 ערוצים, נכנס ברוחב-פס, נבדק **לפני**
  `TUNE_LOCK`). `render_dmr_env(system, multi=True)` מוסיף `DSD_MULTI=1` +
  `DSD_MULTI_GUARD_HZ`/`DSD_MULTI_MAX_RATE_HZ` (מהקבועים ש-
  `_validate_multi_feasible` כבר אימת מולם) + `DSD_AUDIO_TCP_BASE`
  (בלתי-תלוי-מצב, נכתב תמיד). `MODE_SERVICE["multi"]` = אותה יחידת systemd
  כמו `dmr` (**לא** unit נפרד — "SDR אחד בהחלפה" נשמר); `_live_mode` מבדיל
  dmr/multi לפי `state.json` (systemctl לא יכול). `api_mode`/`_boot_restore`
  מטפלים ב-`multi` באותה תבנית כמו `dmr`. `_normalize_dsd` מעדיף
  `phys_freq_hz`/`phys_lcn` (כשקיימים) על `_channelmap_freq(lcn)` — ground-truth
  לא ניחוש. `_dmr_listener`: מפתחות ה-dedup וה-`_slot_open_call` (קורלציית
  הצפנה) מורחבים ל-`(phys_lcn, ...)` — **בלי זה** שתי שיחות בו-זמנית על שני
  ערוצים שונים עם אותו slot היו מתמזגות/מקבלות תג-הצפנה בטעות מערוץ אחר.
  `_rf_ticks`/`_rf_quality_tick`/`_rf_quality_snapshot` מורחבים עם ממד
  `phys_lcn`; `/api/rf` מחזיר גם `by_channel` (החלטת-מוצר: פר-ערוץ **מיום-1**,
  לא נדחה). כל השדות/מפתחות החדשים `None`/ריקים בחד-ערוצי ⇒ **אפס שינוי
  התנהגות** לקוד הקיים.
- **בדיקות:** 37 בדיקות חדשות (140→177): offset-demod + wideband-plan +
  channelmap-parsing ב-`test_rsp_fm.py`; בוני-פקודות + `tag_event` ב-
  `test_dsd_normalize.py`; `render_dmr_env`/`_validate_multi_feasible`/
  `/api/mode multi`/`_live_mode` ב-`test_mode.py`; `phys_lcn` tagging/dedup/
  encryption-per-channel ב-`test_dsd_normalize.py`; `by_channel` RF ב-
  `test_rf_gain.py`. **68/68 ה-fixture replay של `parse_dsd_line` נשאר ירוק
  ללא שינוי אחד** — הפרסור עצמו לא נגע, התיוג קורה אחריו ב-`dsd_pty`.

### ⚠ טרם אומת על חומרה (חסימת-שטח לפני production)
שרשרת האותות הרב-ערוצית (`dsd_pty._run_multi`, `rsp_fm.run_multi`,
`RtlTcpClient`/`rsp_tcp` בקצב-IQ מעל 240kHz הקיים) היא `pragma: no cover` —
לא אומתה על RSP1B אמיתי. הסיכון הטכני המרכזי: קליטת IQ רחבת-פס (עד 2MHz)
מעל `rsp_tcp` (rtl_tcp emulation) לא נבדקה מעולם ביציבות ב-DMR (רק 240kHz
אומת, ר' Phase 5/CHANGELOG). ספייק-מדידה (`scripts/spike_multichannel.sh`
בריפו `DMR-DECREP-SHAHAR`) נכתב לבדיקה על חומרה אמיתית; אם `rsp_tcp` לא יציב
ברוחב-פס הזה, החלופה היא Soapy-direct (`rf/capture.py` ב-DECREP). ר' §5/§10
Phase 7.

### נדחה במכוון ל-Phase הבא
בחירת-ערוצים חלקית (`channel_ids`/`--follow-traffic` — יום-1 מפעיל את **כל**
ה-channelmap של המערכת), ושכבת ה-UI/`channels.json` (טבלת קונפיגורציה + live
status פר-ערוץ) — נדחו עד שהמנוע יאומת על חומרה אמיתית.

## [0.5.0] - גילוי רשתות DMR (frequency discovery)
### Added — מצב `discover`: סריקת תדרים חכמה + גילוי פרמטרים
מצב חדש שסורק טווח RF, מזהה תדרים חשודים כ-DMR (גילוי אנרגיה FFT), ואז בודק כל
מועמד עם DSD-FME ומגלה: תדר בקרה, color code, סוג ערוץ (בקרה/קול/קונבנציונלי),
LSN-ים ו-TG-ים פעילים. תוצאה: דוח גילוי + "שמור כמערכת" (מיזוג ל-systems.json).

- **`webtune/discovery.py` (מודול חדש, לוגיקה טהורה):** `validate_sweep_plan`
  (ברירת מחדל 450–470 MHz, תחום 24–1300, רוחב עד 100MHz), `build_freq_grid`
  (צעד לפי חלון שמיש ‎~1.8MHz), `detect_candidates` (סף אדפטיבי median+k·MAD עם
  מרווח-מינימום יחסי, מסכת DC spike מרכזי, crop קצוות, מיזוג בינים לפי רוחב ערוץ
  12.5kHz), `aggregate_probe` (אירועים→רשומת רשת: is_dmr/cc/channel_type/rest_lsns/
  talkgroups/rids/encrypted/confidence), `discovery_to_system`.
- **`webtune/rsp_fm.py`:** `compute_power_spectrum` (Hann→FFT→dBFS, טהור ונבדק),
  מצב `--sweep` (מדלג על ה-NFM demod, gain ידני קבוע, FFT על ה-IQ הגולמי עם
  איפוס ב-generation change), verb `SPECTRUM` בשרת ה-rigctl, `set_fixed_gain`.
- **`webtune/dsd_pty.py`:** `parse_dsd_line(..., emit_status=True)` מוסיף אירועי
  `sync` (‎`Sync: +DMR ... Color Code=NN | state`) ו-`channel_status` (Rest LSN +
  LSN states) — שדות ש-DSD-FME כבר מדפיס אך נזרקו. **ברירת המחדל (emit_status=False)
  ללא שינוי** — פרסור רגיל זהה byte-for-byte, ה-fixture replay (68/68) נשאר ירוק.
  מצב sweep ב-`_run` (רק rsp_tcp+rsp_fm, בלי DSD-FME).
- **`webtune/app.py`:** תזמור `_discover_activate`/`_discover_loop`/
  `_discover_stop_thread` (מודל scan; TUNE_LOCK per-step בלבד; job חולף בזיכרון —
  לא מַתמיד ב-state, לא משוחזר ב-boot), collector side-tap ב-`_dmr_listener`
  (מתויג-epoch, לא נוגע ב-dedup), `render_dmr_env` עם `trunk` פר-מערכת (במקום
  קבוע) + דגלי sweep/emit_status, `_probe_system` (non-trunk), `discovery.json`.
  נקודות REST: `POST /api/mode {mode:"discover"}`, `GET /api/discover`,
  `POST /api/discover/save`. `api_state`/`api_health` מדווחים discover כשפעיל.
- **UI:** תצוגת "🔎 גילוי" חדשה — טופס טווח, התקדמות חיה (שלב/progress/תדר),
  טבלת רשתות מגולות + "שמור כמערכת". הערה מפורשת על מיפוי-ערוצים חלקי.
- **בדיקות:** `tests/test_discovery.py` (33 בדיקות: טהור + Flask + e2e ממוקף),
  `compute_power_spectrum` ב-`test_rsp_fm`, אירועי sync/channel_status ב-
  `test_dsd_normalize`. סה"כ 140 בדיקות ירוקות.

> ⚠ **גבול אימות חומרה:** שרשרת האותות של הסריקה (מצב sweep ב-rsp_fm/dsd_pty,
> לקוח ה-rigctl/spectrum ב-app.py, בדיקת DSD-FME) היא `pragma: no cover` —
> מתאמתת רק על Pi 5 + RSP1B אמיתי, כמו הגשר של 0.4.0. CI מכסה את כל הלוגיקה
> הטהורה, `compute_power_spectrum`, שינויי ה-parser, ונקודות ה-Flask. הזמנים/
> הרגישות בשטח (התייצבות retune, יציבות gain, peaks אמיתיים) ייאומתו על חומרה.
>
> **מיפוי LCN↔תדר אינו ניתן לגילוי אוטומטי מלא:** Cap+ משדר LSN לוגי (לא תדר),
> ו-SDR יחיד לא יכול לצפות בבקרה ובקול בו-זמנית — הדוח מדווח LSN-ים שנצפו + CC +
> תדר בקרה; מפת הערוצים המלאה נשלמת ידנית.

## [0.3.1] - תיקון-ביניים: מעבר ל-dsd-neo (הוחזר ב-0.4.0, ר' למטה)
> ⚠ **המעבר ל-dsd-neo בגרסה הזו הוחזר ב-0.4.0** — הבינארי בפועל מ-0.4.0 ואילך
> הוא שוב `lwvmobile/dsd-fme`, מוזן דרך גשר IQ→PCM+rigctl עצמאי במקום לעבור
> binary. **הממצא המרכזי כאן נשאר נכון ותקף:** `lwvmobile/dsd-fme` אכן אינו
> תומך בקלט `‎-i rtltcp:` ישיר — זו הסיבה שהוחלט שלא לחזור לחיבור הישיר
> המקורי, אלא לפתור את זה בגשר עצמאי.

### Fixed — מעבר ל-dsd-neo (נתפס בהרצה ראשונה על חומרה אמיתית, Pi5+RSP1B)
ההרצה הראשונה על חומרה אמיתית חשפה שתי שכבות של תקלות ב-`lwvmobile/dsd-fme`:

- **שלב 1 (נתפס):** `DSD_FME_BRANCH` הצביע ל-`main`, שלא קיים בכלל בריפו
  (הענף הפעיל האמיתי הוא `audio_work`) — `git clone` נכשל מיד בשלב 5.
- **שלב 2 (נתפס אחרי תיקון שלב 1, מ-journalctl של `dmr-dsdfme.service`):**
  גם עם הענף הנכון, `dmr-dsdfme.service` קרס מיד (`exit 1`). בדיקה של הפקודה
  שבאמת רצה חשפה `build_command()` שבור: דגל `‎-C` כפול (בלוק `DSD_COLOR_CODE`
  ישן הזריק `‎-C` בטעות — ל-DMR *אין* דגל CLI לצבע-קוד, הוא מזוהה מהסנכרון
  בפועל) ודגל `‎-c <תדר>` שלא קיים בכלל באף fork (אומת מול תיעוד CLI מלא).
  חקירה נוספת גילתה בעיה עמוקה יותר: `lwvmobile/dsd-fme` **לא תומך כלל** בקלט
  `‎-i rtltcp:` (רק `‎-i tcp:` הקנייני של SDR++ ו-`‎-i rtl:` USB מקומי) — כל
  ארכיטקטורת הגשר `rsp_tcp` מ-Phase 1 נשענה על הנחה שגויה.
  **הפתרון:** מעבר ל-`arancormonk/dsd-neo` (v2.3.0) — fork פעיל ומתועד היטב
  שתומך `‎-i rtltcp:host:port` במפורש (מתאים בדיוק לגשר `rsp_tcp` הקיים,
  שנשמר ללא שינוי) וגם SoapySDR ישיר (לא מנוצל כרגע — שיפור ארכיטקטוני עתידי
  מתועד, לא יושם תחת לחץ זמן). תוקן: `install.sh` (בלוק בנייה חדש עם
  `ninja-build`/`libfftw3-dev`/`libblas-dev`/`liblapack-dev`/`gfortran`/
  `libssl-dev`/`libcurl4-openssl-dev` שדסק-נאו דורש, `cmake --install`),
  `dsd_pty.build_command` (הוסר `‎-C` הכפול ו-`‎-c`; `‎-6 <dir>` (WAV רציף)
  הוחלף ב-`‎-7 <dir> -P` הנכון (WAV per-call), `DSD_BIN` ברירת מחדל `dsd-neo`).
  תדר ערוץ הבקרה כבר לא דגל CLI — `app.render_channelmap` מזריק אותו כשורת
  "בוא לכאן קודם" ראשונה בקובץ ה-channelmap עצמו (ערוץ-דמה `999`, לפי מוסכמת
  התיעוד הרשמי של dsd-neo).
- **נוסף:** `dsd_pty._run` היה בולע את הפלט הגולמי של DSD-FME/`rsp_tcp` (רק
  אירועים מפורסרים נשלחו הלאה) — שגיאות/קריסה של המפענח לא הגיעו ל-
  `journalctl -u dmr-dsdfme` כלל, מה שהקשה מאוד על האבחון בפועל. כעת כל שורת
  פלט גולמית מהודהדת ל-stderr (=> journal), ו-`rsp_tcp` יורש stdout/stderr
  במקום `DEVNULL`.

## [0.4.0] - תיקון קריסת dmr-dsdfme + גשר IQ→PCM עצמאי (✅ אומת על חומרה אמיתית)
### Fixed — קריסת `dmr-dsdfme` תוך שניות מהעלייה
`rsp_tcp` היה נופל (`strmHandlerThread`/`heartBeatThread`/`eventHandlerThread: Exit`,
`sdrplay_api_Close`) תוך שניות מהתחברות DSD-FME אליו כלקוח rtl_tcp ישיר
(`-i rtltcp:...`) — חוסר-תאימות ידוע בין לקוח ה-rtl_tcp של DSD-FME לבין ה-emulator
של SDRplay. הפתרון מסיר את החיבור הישיר לגמרי במקום להטליא אותו.

**מחליף את 0.3.1:** גרסה זו נוסתה גם עם `arancormonk/dsd-neo` (ר' 0.3.1 למעלה),
שנבחר כי הוא תומך `‎-i rtltcp:` ישיר — אך הוחלט להחזיר את `lwvmobile/dsd-fme`
ולפתור את חוסר-תמיכת ה-rtltcp בגשר עצמאי (`webtune/rsp_fm.py`) במקום להחליף
בינארי, כדי לשמר את עבודת הפרסור/הנרמול הקיימת (`parse_dsd_line`,
`tests/fixtures/capplus_slco_sample.csv`) שתלויה בפורמט הפלט הספציפי של
lwvmobile/dsd-fme.

### Changed — ארכיטקטורה: rsp_tcp → rsp_fm.py (IQ→PCM+rigctl) → DSD-FME
- **`webtune/rsp_fm.py` (חדש, תלות NumPy):** דמודולטור NFM עצמאי (IQ u8 240kHz →
  PCM signed-16 48kHz, FIR 121-taps + DC-blocker stateful חד-קוטבי) + שרת rigctl
  לכיוונון טראנקינג + שרת בקרת-רווח (unix socket).
- **`dsd_pty.py`:** מפקח כעת על 3 תהליכי-בן (rsp_tcp, rsp_fm.py, DSD-FME תחת PTY)
  במקום 2. DSD-FME עבר מ-`-i rtltcp:...` ל-`-i tcp:...` (קלט אודיו) + `-U` (rigctl)
  — דפוס השימוש הרשמי של lwvmobile/dsd-fme לרדיו בלי טראנקינג IQ נטיבי. תוקן גם
  באג ותיק בהקלטות per-call (`-6` השגוי → `-7 <dir> -P` הנכון).
- **`install.sh`:** `DSD_FME_BRANCH` עבר מ-`main` (שוב לא קיים ב-upstream) ל-`audio_work`
  (ברירת המחדל הנוכחית של lwvmobile/dsd-fme, שם חיות `-i tcp`/`-U`/`-7`/`-P`).
- **`app.py`:** `render_dmr_env` כולל כעת גם את קבועי הגשר (`DSD_RTLTCP`/`DSD_AUDIO_TCP`/
  `DSD_RIGCTL`/`DSD_IQ_RATE`/`DSD_AUDIO_GAIN`) — בלי זה הם נדרסים מתוך `/etc/dmr/dmr.env`
  בכל מעבר מצב (`_enter_dmr`/מעבר-רגל-סריקה).
- **נוד-רווח (`/api/gain`):** מגיע כעת בפועל ל-SDR (`rsp_fm.RtlTcpClient.nudge_gain`
  → פקודות rtl_tcp אמיתיות ל-`rsp_tcp`) במקום הקשה מדומה ל-DSD-FME, שכבר לא נוגע ב-SDR.

### Fixed — חיזוקי אמינות בגשר החדש
- `RtlTcpClient` היה בלי read-timeout אחרי handshake (`settimeout(None)`) — אם
  `rsp_tcp` נתקע בלי לסגור את הסוקט, הגשר היה נתקע לנצח וה-health-check לא תופס
  את זה. עכשיו timeout קבוע (5s) הופך תקיעה לשגיאה מטופלת (restart).
- דמודולטור ה-DC-blocking עבר מ"ממוצע בלוק" (מחושב מחדש בנפרד על כל chunk של
  ~100ms — קפיצה בכל גבול chunk) לפילטר IIR חד-קוטבי עם מצב הנשמר לרוחב chunks,
  כמו ה-FIR overlap הקיים.
- שליחת PCM ל-DSD-FME (`AudioSender`) עברה ל-thread נפרד עם תור חסום — במקום לחסום
  את ה-thread שקורא IQ מ-`rsp_tcp` (עד 2 שניות אם DSD-FME נתקע).
- שלושת תהליכי-הבן (`rsp_tcp`/`rsp_fm.py`/DSD-FME) מקבלים כעת `PR_SET_PDEATHSIG`
  — הגנה מפני תהליכים יתומים שממשיכים להחזיק את ה-SDR/פורטים אם המפקח עצמו נופל
  (למשל OOM-kill), מה שהיה גורם ל-restart הבא להיכשל באותה צורה.

### ✅ אומת על חומרה אמיתית (Pi 5 + SDRplay RSP1B)
כל שרשרת האותות (`rsp_tcp`→`rsp_fm.py`→DSD-FME) היא `pragma: no cover` — לא
נבדקת ב-CI; pytest ירוק (107/107) מוודא רק לוגיקה טהורה (argv, פרסור, דמודולטור
מול אות מסונתז). **אומת בנוסף בפועל** על Pi 5 + RSP1B: לאחר `sudo ./install.sh`,
`dmr-dsdfme.service` נשאר `active (running)` יציב (בעבר קרס תוך שניות), כל
תהליכי-הבן (`rsp_tcp`/`rsp_fm.py`/DSD-FME) חיים ב-cgroup אחד, ו-DSD-FME מתחבר
בהצלחה ל-audio socket ("TCP Connection Success!") ומתחיל תהליך פענוח/טראנקינג
מול רשת Cap+ אמיתית.

## [0.3.0] - 2026-07-12
### Changed — פרסור DSD-FME מבוסס קליטה אמיתית (לא ניחוש)
עד גרסה זו `dsd_pty.parse_dsd_line` היה בנוי על ניחוש סביר של פורמט הפלט של
DSD-FME. בגרסה זו הוא **נכתב מחדש מול קליטה אמיתית** — 20,000 שורות מרשת
Motorola Capacity Plus רב-אתרית (SLCO) — ואומת ב-replay מלא: **כל אחת מ-68
הצורות הייחודיות** בקליטה (`tests/fixtures/capplus_slco_sample.csv`) מסווגת
נכון (100% התאמה, כולל housekeeping שנופל).

- **ממצא מרכזי:** ~80% מהפלט האמיתי הוא רעש תפעולי (`lsn_status`/
  `channel_status`/`site_info`/`ip_mapping`/`bank_call`/`preamble_csbk`) —
  `parse_dsd_line` כעת **מטיל את אלה החוצה במקור** (מחזיר `None`, לא נשלח
  ב-UDP כלל) במקום להציף את `dmr.jsonl`.
- **תיקון סמנטי:** אין שדה TG נפרד בפרוטוקול DMR — בשיחת קבוצה `TGT` *הוא*
  ה-talkgroup עצמו; בשיחה פרטית `TGT` נשאר יעד. `_normalize_dsd` תוקן בהתאם.
- **הצפנה:** DSD-FME לא הדפיס בקליטה שנבדקה שם אלגוריתם/מזהה מפתח (FLCO/FID
  הם routing fields, לא ALG/KEY) — אירוע `encryption` **מתואם** (לא כרטיס
  עצמאי) לשיחת הקול הפתוחה באותו slot (`_slot_open_call`, חלון 15ש'), ומסמן
  `encrypted=True` בלבד. לעולם לא ממציאים שם אלגוריתם.
- **תדר:** DSD-FME לא מדפיס תדר בקליטה אמיתית — `freq` על כרטיס נגזר כעת
  מ-channelmap המערכת הפעילה (`_channelmap_freq`, לפי LCN/Rest-LSN), לא מטקסט.

### Added — איכות RF (תדירות שגיאות) + נוד-רווח חי
- **`_rf_quality_tick`/`_rf_quality_snapshot`:** DSD-FME לא נותן שום SNR/RSSI/
  dBFS רציף — רק אירועי CRC/FEC בודדים (`SLCO_CRC`/`CACH_BURST_FEC`/
  `CSBK_CRC`/`CSBK_FEC`, וגם פריימי-קול עם `(CRC ERR)`). חלון נגלל של 60
  שניות סופר תדירות שגיאות אמיתית (`errors_per_min` + פילוח לפי סוג) —
  **לא ממציאים יחס/SNR**. `GET /api/rf`.
- **נוד-רווח חי (`dsd_pty.send_gain_nudge`):** DSD-FME תומך בהקשות מקלדת
  `g`/`G` ("Manually decrease/increase RTL gain") בזמן ריצה, שנשלחות דרך
  ה-unix socket הקיים (`DSD_CTRL_SOCK`) — **בלי לעצור את DSD-FME ובלי פטצ'
  קוד C**. מונה יחסי best-effort ב-state (`gain_nudge`, מתאפס בכל
  `_enter_dmr`) — אין readback אמיתי מ-DSD-FME. `POST /api/gain
  {direction: up|down}`.
- **UI:** כרטיס "📶 איכות RF ובקרת רווח" בבית — תדירות שגיאות + פילוח לפי
  סוג + כפתורי נוד-רווח, עם הבהרה מפורשת שאין מד dBFS/SNR רציף.
- **נדחה במכוון (Phase עוקב):** מד dBFS עצמאי מה-SDR עצמו דורש פטצ' קוד C על
  `rsp_tcp` (SDRplay API הוא single-owner; אין side-channel telemetry
  בגרסה הסטנדרטית) — לא ניתן לאמת בלי חומרה אמיתית (RSP1B). ר' CLAUDE.md §8.

### Tests
- `tests/fixtures/capplus_slco_sample.csv` — 68 הצורות הייחודיות מהקליטה
  האמיתית (type+pattern), עם replay-test שמוודא 68/68 סיווג מדויק.
- `tests/test_dsd_normalize.py` נכתב מחדש (32 בדיקות) מול הפורמט האמיתי.
- `tests/test_rf_gain.py` (7 בדיקות) — שכבת ה-HTTP של `/api/rf`/`/api/gain`.
- סה"כ 106 בדיקות (היו 85).

## [0.2.0] - 2026-07-12
### Added — Phase 2: אנליטיקה (הצפנה + תעבורה)
- `_encryption_stats`: היסטוגרמת אלגוריתמי הצפנה (ALG) + %מוצפן פר-talkgroup —
  לעולם לא מפענח, רק מסכם את התג (`encrypted`/`enc.alg_name`) הקיים בכל כרטיס.
- `_traffic_stats`: air-time + מספר שיחות פר-TG, והתפלגות שעתית (0–23, שעון
  מקומי) לזיהוי שעות עומס.
- `GET /api/analytics/encryption` ו-`GET /api/analytics/traffic` — אותם
  פרמטרים כמו `/api/dmr` (`?day=`/`?all=1`, ברירת מחדל: היום).
- UI: כרטיסיית **📊 ניתוח** חדשה — היסטוגרמת ALG, בר מוערם ברור/מוצפן פר-TG
  (עם legend), heatmap שעתי (24 תאים, גוון accent יחיד — sequential), ורשימת
  TG-ים מובילים לפי זמן-שידור. טוקני `--st-good`/`--st-critical` עברו ולידציה
  מול `validate_palette.js` (skill dataviz) לפני שימוש.

### Added — Phase 3: רשת ומיקום (גרף RID↔TG + מפת LRRP)
- `_rid_tg_graph`: צירי source-RID→talkgroup ממושקלים במספר שיחות (who-talks-
  to-whom), רק שיחות קבוצה. `GET /api/analytics/graph`.
- `_lrrp_snapshot`: מיקום אחרון-ידוע פר-RID מאירועי LRRP שבזיכרון (בהשראת
  `adsb.aircraft_snapshot` ב-AIR-AM — "עכשיו" בלבד, לא ארכיון). `GET /api/positions`.
- UI: רשימת גרף RID↔TG מדורגת לפי משקל, ומפת Leaflet (vendored, lazy-load)
  שמציגה marker לכל RID עם מיקום LRRP ידוע. ריק בשקט כשהרשת לא שולחת LRRP
  סטנדרטי (Motorola proprietary אינו מפוענח ע"י DSD-FME).

### Fixed
- **RTL:** ה-UI כולו היה חסר `<html lang="he" dir="rtl">` (נשמט ב-Phase 1) —
  התגלה בבדיקת Playwright חזותית של מסך האנליטיקה. תוקן; משפיע על כל התצוגות.
- **מעבר לתצוגת שיחות** לא רינדר מיידית הודעות שכבר נטענו לזיכרון — המתין
  לטיק ה-polling הבא (עד 3 שניות). `showView("calls")` מרנדר+מרענן כעת מיידית
  (אותה תבנית שכבר הייתה קיימת למעבר לתצוגת אנליטיקה).

### Tests
- `tests/test_analytics.py` (14 בדיקות): `_encryption_stats`/`_traffic_stats`/
  `_rid_tg_graph`/`_lrrp_snapshot` + כל 4 ה-routes החדשים, כולל ולידציית `?day=`.
  סה"כ 85 בדיקות.

## [0.1.0] - 2026-07-12
### Added — Phase 1: יסוד עובד קצה-לקצה
הפרויקט נוצר משכפול ארכיטקטורת **AIR-AM** והחלתה על פענוח **רשתות DMR** (במיוחד
Motorola Capacity Plus) באמצעות **DSD-FME**, על Raspberry Pi 5 + SDRplay RSP1B,
בשליטה מלאה מהטלפון בדפדפן.

- **מתאם DSD-FME → JSON (`webtune/dsd_pty.py`):** מריץ את DSD-FME תחת PTY ואת גשר
  `rsp_tcp` (SDRplay→rtl_tcp) כתהליך-בן; מפרסר את הפלט הטקסטואלי (`parse_dsd_line`)
  ושולח כל אירוע כ-JSON ב-UDP — הופך את DSD-FME ל"API-first" כמו `acarsdec -j`.
- **שרת הבקרה (`webtune/app.py`):** מצב `dmr` (DSD-FME) / `off` (standby) / `scan`
  (סבב בין מערכות) — SDR אחד בהחלפה, מתזמר-web, boot-restore, כישלון-נופל-ל-off.
  `_normalize_dsd` ממיר אירוע DSD-FME לכרטיס שיחה אחיד (TG/RID/slot/CC/enc/BER);
  listener על UDP 5555 → `dmr.jsonl` (retention + ארכיון יומי + ייצוא CSV/JSON).
- **מערכות DMR (`systems`):** פריסטים של רשתות (תדר בקרה + color code + מפת LCN),
  נערכים מהטלפון, נשמרים ב-`systems.json`.
- **אליאסים (`webtune/aliases.py`):** שמות ל-TG/RID מייבוא CSV (RadioID.net user.csv)
  + עריכות ידניות מהטלפון; join אוטומטי בכל שיחה.
- **רוסטר מאוחד:** היתוך שיחות לפי RID/TG (בסיס לגרף RID↔TG של Phase 3), חי בכל מצב.
- **הקלטות per-call:** DSD-FME כותב WAV לכל שיחה; watcher + retention + נגן בדפדפן;
  תמלול whisper אופציונלי (כבוי כברירת מחדל, `INSTALL_DMR_WHISPER=1`).
- **פריסה:** `install.sh` (SDRplay API, SoapySDRPlay3, mbelib, DSD-FME, rsp_tcp,
  משתמש `dmr` לא-root + sudoers ממוקד), יחידות systemd (`sdrplay`/`dmr-dsdfme`/
  `dmr-web`), `dmr-wait-sdrplay`, `udev/99-dmr.rules`.
- **UI (`static/index.html`):** PWA בעברית/RTL — בית (מצבים, עורך מערכות, אליאסים,
  רוסטר, בריאות "האם אני מפענח") + תצוגת שיחות (פיד חי, פילטרים, ארכיון, ייצוא, נגן).
- **בדיקות (`tests/`):** 71 בדיקות, SDR/systemd ממוקפים — נרמול (parse_dsd_line +
  `_normalize_dsd`), מעברי מצב, boot-restore, סריקה, אליאסים, הקלטות, אבטחה, ארכיון.
  CI: pytest (Python 3.11) + `bash -n` על install.sh ו-dmr-wait-sdrplay.

### שלבים עוקבים (מתוכננים)
- **Phase 2:** ניתוח הצפנה (היסטוגרמת ALG, %מוצפן פר-TG) + אנליטיקת תעבורה (heatmap).
- **Phase 3:** גרף RID↔TG (who-talks-to-whom) + מפת GPS/LRRP (Leaflet).

[Unreleased]: https://github.com/Shahar373/DMR/compare/v0.3.0...HEAD
[0.3.0]: https://github.com/Shahar373/DMR/compare/v0.2.0...v0.3.0
[0.2.0]: https://github.com/Shahar373/DMR/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/Shahar373/DMR/releases/tag/v0.1.0
